# 巡检系统（xunjian） ↔ network-seek 拓扑联动 接口文档

> 本文件同时存放在两个仓库根目录：
> - 巡检系统：`xunjian_system1/联动接口文档_巡检与拓扑.md`
> - 拓扑系统：`network-seek/联动接口文档_巡检与拓扑.md`
>
> 更新日期：2026-07-23（阶段四收尾）｜ 维护人：开发侧
>
> 本文档定义**两系统之间的解耦契约**：巡检系统负责把设备 CLI 采集结果导出为标准 fixture 文本，
> network-seek 负责把 fixture 摄入为 Neo4j 资产拓扑图。中间通过「文件」解耦，互不依赖对方运行时。

---

## 1. 系统定位

| 系统 | 技术栈 | 职责 |
|---|---|---|
| 巡检系统 `xunjian_system1` | Django + MySQL | 设备 CLI 采集 → `CheckResult` 表 → CMDB 台账（`CmdbDevice`）→ 导出 fixture |
| 拓扑系统 `network-seek` | Python + Neo4j | fixture → 解析为厂商无关 IR → 写入 Neo4j 图 → 对外提供 MCP 查询工具 |

两者关系：**巡检系统是数据生产者，network-seek 是数据消费者**。耦合点（阶段三起）：
- 默认：`cmdb_fixtures/<site>/<device>.json` 结构化 JSON（`schema_version` + `parsed` 命令→规范化 dict），由 `import_comware_structured` 薄导入，**不再 re-parse raw**；
- 回退：`--raw` 另产 `cmdb_fixtures/<site>/<device>.txt`（旧版 `!Command:` 分段），由 `reimport_comware.py --no-json` 导入。
JSON 契约字段与《解析器单一真源与采集时一次解析_设计文档.md》§4 一致。

---

## 2. 整体数据流（联动链路）

```
[设备 CLI 采集]  (netmiko/paramiko, 按角色模板命令集)
        │
        ▼
  CheckResult 表  (device, command, result, created_at)
        │
        ├─► sync_cmdb  ──────────────►  CmdbDevice  (CMDB 台账：model/cpu/mem/serial/uptime...)
        │
        └─► export_networkseek_fixtures
                    │
                    ▼
            cmdb_fixtures/<site>/<device>.json   (每台设备一个文件，结构化 JSON；--raw 另产 .txt)
                    │
                    │  [rebuild_topology --push 触发，默认 --json]
                    ▼
            scripts/reimport_comware.py  ──►  full_reset_and_import_comware_json
                    （回退：reimport_comware.py --no-json ──► full_reset_and_import_comware）
                    │
                    ▼
            Neo4j 图 (Device / Interface / VLAN / CONNECTS_TO / Route / ACL / VRF ...)
                    │
                    ▼
            MCP 工具 (14 个) 对外查询资产拓扑
```

> 说明：图采用**全量重建**（clear_all 后重灌），因为图是「设备当下真实状态快照」，
> 全量比增量 diff 简单且不会脏；百台规模完全够用。

---

## 3. 接口契约：导出格式（巡检系统 → network-seek）

> **阶段三起，默认契约是「结构化 JSON」**；旧版 raw `.txt` 仅作调试/回退（需 `export --raw` 显式产出）。
> JSON 的 `parsed[命令]` 取值即《解析器单一真源与采集时一次解析_设计文档.md》§4 的规范化 schema，
> 由 xunjian 在采集时**一次解析**落库（`DeviceParseResult`），network-seek 直接消费、**不再 re-parse raw**。

### 3.1 文件布局

```
<xunjian BASE_DIR>/cmdb_fixtures/
├── 知识城/
│   ├── fw001.a.pri.zscidc2f1.gzxc-hlw.json      （默认：结构化 JSON）
│   └── ...
└── 化龙/
    ├── fw001.pri.2IDC4f.hualong.xc.json
    └── ...
```

- **每台设备一个 `.json` 文件**，文件名 = 设备名经 `re.sub(r'[^\w\-.]', '_', name)` 转义
  （域名中的点号 `.` 会被保留，其它非单词字符转 `_`）。
- 子目录 = 站点名（`dev.site`）。
- 调试/回退用的 `.txt`（raw 文本，见 §3.3）仅在 `export --raw` 时**并行**产出，与 `.json` 同名不同扩展名。

### 3.2 结构化 JSON 格式（默认，阶段三起）

顶层字段：

```jsonc
{
  "device": "csw001",          // 台账名（= NewDevice.name，主键）
  "site": "知识城",            // 站点
  "role": "CSW",              // 角色
  "mgmt_ip": "10.x.x.x",      // 管理 IP
  "schema_version": "1",      // 契约版本号（与 xunjian app02/parsers.SCHEMA_VERSION 一致）
  "parsed": {                 // 命令 → 规范化 dict（设计文档 §4 契约）
    "display version":                  { "model": "...", "os_version": "...", "uptime_days": 312 },
    "display interface brief":          [ { "name": "GE1/0/1", "status": "up", "protocol": "up" } ],
    "display lldp neighbor-information list": [
        { "src_intf": "GE1/0/1", "dst_device": "psw004", "dst_intf": "GE1/0/24" } ],
    "display vlan brief":               [ { "id": 1, "name": "default" }, { "id": 100, "name": "" } ],
    "display current-configuration":    { "sysname": "...", "vlans": [...], "ips": [...], "acls": [...] },
    "display ip routing-table":         [ { "dest": "0.0.0.0/0", "proto": "Static", "nexthop": "10.0.0.1" } ],
    // 高可用 / 堆叠 / 安全（同一份 schema）
    "display vrrp":                     [ { "group_id": 1, "role": "master", "virtual_ip": "...", "master_ip": "..." } ],
    "display irf":                      { "name": "IRF-1", "members": [ ... ] },
    "display m-lag summary":            { "peer": "...", "groups": [ ... ] },
    "display link-aggregation verbose": [ { "lag_name": "Bridge-Aggregation100", "local_ports": [ ... ] } ],
    "display security-zone":            [ { "name": "Trust", "interfaces": [ ... ] } ],
    "display security-policy ip":       [ { "rule_id": 1, "name": "OSPF", "source_zones": [ ... ], ... } ],
    "display remote-backup-group status": { "role": "Primary", "state": "Dual-active", ... }
  }
}
```

**约定**：
- `parsed` 的键 = `SECTIONS` 中的命令名（与设计文档 §8.1 导出命令集、旧版 `.txt` 的 `!Command:` 段头**完全一致**）。
- 某命令无采集/解析结果时，该键**直接不出现在 `parsed` 中**（而非留空对象）——network-seek 侧按 key 存在性建图元素，天然安全。
- `parsed[cmd]` 的取值结构详见《解析器单一真源与采集时一次解析_设计文档.md》**§4 规范化结构化 Schema**，该 schema 两仓共同依赖，解析器升级时 bump `schema_version`。

### 3.3 旧版 raw `.txt` 格式（回退，需 `export --raw`）

仅用于排错或降级到旧导入链路（`reimport_comware.py --no-json`）。单设备含多命令，
按 `!Command: <命令>` 分段，段间用**两个空行**分隔：

```
!Command: display version
<display version 真实回显>

!Command: display current-configuration
<display current-configuration 真实回显>

!Command: display interface brief
<...>
```

段头关键词匹配规则（`network_seek.parsers.comware._cmd_to_key`，仅旧链路用）：
按关键词模糊匹配、大小写不敏感：`version` / `current-configuration` / `interface` / `lldp` /
`routing-table` / `vlan`。**段头命令字符串中必须包含对应关键词**，否则该段被丢弃、
相关图元素缺失。建议段头写全（与 `SECTIONS` 完全一致）。

### 3.4 命令 ↔ 解析器 ↔ 图节点 映射表（核心契约）

解析**只在 xunjian 发生一次**（单一真源 `app02/parsers/comware.py`）；
network-seek 仅做「dict → pydantic IR → 图」的薄映射，不再 re-parse raw。

| 巡检命令（`SECTIONS` / JSON `parsed` 键） | network-seek 薄映射函数 | 产出图元素 |
|---|---|---|
| `display version` | `_build_version_comware` | `Device`(model/os_version/serial/uptime) |
| `display current-configuration` | `_build_running_config_comware` | `VLAN` / 接口-VLAN 绑定 / 三层 `IPSubnet` / `ACL` / `SecurityRule` / `VRF` |
| `display interface brief` | `_build_interfaces_comware` | `Interface`(状态/速率/双工/VLAN) |
| `display lldp neighbor-information list` | `_build_lldp_comware` | `CONNECTS_TO` 链路边（含对端端口） |
| `display vlan brief` | `_build_vlan_brief_comware` | `VLAN` 节点（兜底，重复则跳过） |
| `display ip routing-table` | `_build_route_table_comware` | `ROUTES_TO` 路由边 |
| `display vrrp` | `_build_vrrp_comware` | `VRRPGroup` / `VRRP_MASTER` 边 |
| `display irf` | `_build_irf_comware` | `Stack` 节点 |
| `display m-lag summary` + `display link-aggregation verbose` | `_build_mlag_*` / `_build_lag_*` | `MLAG` / `LAG` / `MEMBER_OF_LAG` |
| `display security-zone` | `_build_security_zone_comware` | `SecurityZone` / 接口入域 |
| `display security-policy ip` | `_build_security_policy_comware` | `SecurityPolicy` / 域间策略边 |
| `display remote-backup-group status` | `_build_rbm_status_comware` | RBM 双机热备状态 |

### 3.5 编码

- 巡检系统导出 JSON / `.txt` 均为 **UTF-8**。
- network-seek 读取 `.txt` 时先试 UTF-8，失败回退 **GBK**（`errors='replace'`）；
  读取 JSON 时按标准 `json.load`（UTF-8）。

---

## 4. 调用方式（命令接口）

### 4.1 巡检系统侧（一键编排）

```bash
# 刷新全部站点 CMDB 台账 + 导出 fixture（不推图）
python manage.py rebuild_topology

# 仅某站点
python manage.py rebuild_topology --site 知识城

# 顺带推送刷新 Neo4j 拓扑图（需正确配置 settings）
python manage.py rebuild_topology --push

# 覆盖 network-seek 目录（不解 settings）
python manage.py rebuild_topology --push --network-seek-dir <路径>

# 也可分步手动执行：
python manage.py sync_cmdb [--site X]                       # CheckResult → CmdbDevice
python manage.py export_networkseek_fixtures [--site X] [--out 目录]   # → 结构化 JSON fixture（--raw 另产 .txt）
```

`rebuild_topology` 内部等价于：`sync_cmdb` → `export_networkseek_fixtures`（默认 JSON）→ （`--push` 时）
遍历 `cmdb_fixtures/<site>/` 逐个调用 `scripts/reimport_comware.py --json`（旧路径：`--legacy-fixture` / `--no-json`）。

### 4.2 network-seek 侧（被调用）

```bash
# 阶段三默认：结构化 JSON 薄导入（不再 re-parse raw）
python scripts/reimport_comware.py \
    --dir cmdb_fixtures/知识城 --json \
    --bolt bolt://localhost:7687 \
    --user neo4j --pwd networkseek2024

# 回退：旧版 .txt（需 export 时加 --raw）
python scripts/reimport_comware.py --dir cmdb_fixtures/知识城 --no-json ...
```

函数签名：`full_reset_and_import_comware_json(conn, json_dir)`（结构化）/ `full_reset_and_import_comware(conn, fixtures_dir)`（txt），内部均 `clear_all()` 后全量导入。

---

## 5. 配置项（两侧约定）

### 5.1 巡检系统 `xunjian_system1/settings.py`

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `NETWORK_SEEK_DIR` | `C:/Users/ZSS/Desktop/network-seek` | network-seek 项目根（推图脚本所在） |
| `NETWORK_SEEK_PYTHON` | `C:/Users/ZSS/Desktop/network-seek/venv/Scripts/python.exe` | 执行 `reimport_comware.py` 的解释器 |
| `NEO4J_BOLT_URI` | `bolt://localhost:7687` | 推图 Neo4j bolt 地址 |
| `NEO4J_USER` | `neo4j` | Neo4j 用户 |
| `NEO4J_PASSWORD` | `networkseek2024` | Neo4j 密码 |

### 5.2 network-seek 侧

- `Neo4jConnection(uri, user, password)` 默认 `bolt://localhost:7687` / `neo4j` / `networkseek2024`
  （见 `network_seek/graph/connection.py`）。
- `scripts/reimport_comware.py` 的 `--bolt/--user/--pwd` 默认值同上。

> ⚠️ **配置注意**：`rebuild_topology --push` 当前只用**单一** `NEO4J_BOLT_URI`（默认 7687）。
> `settings.py` 注释提到「知识城 7687 / 化龙 7688 两容器」，但代码**未实现按站点选端口**，
> 如需多站点不同端口，需扩展或手动分次调用。

---

## 6. 数据契约：字段映射

### 6.1 巡检采集 → CMDB（`CmdbDevice`，由 `sync_cmdb` 写入）

解析 `display version`（`sync_cmdb.parse_version`）：

| 来源正则 | 写入字段 | 备注 |
|---|---|---|
| `H3C\s+(.+?)\s+uptime` | `model` | **多 token**，兼容防火墙 `SecPath M9000-X10`；失败回退 `NewDevice.device_type`（字面量，如 `hp_comware`） |
| `Comware Software, Version X, Release Y` | `os_version` | 如 `Comware 7.1.064 9900P31` |
| `Device name:\s*(\S+)` | `name` | 真实回显常缺失 |
| `SN:\s*(\S+)` | `serial` | 另由 `display device manuinfo` 经 `parse_manuinfo` 补充（序列号采集闭环） |
| `uptime is N week(s) M day(s)` | `uptime_days` | |

> CMDB 的资源水位（CPU/内存）来自巡检 check 结果，非 `display version`。

### 6.2 导出 fixture → Neo4j 图节点（阶段三起：默认消费结构化 JSON）

默认链路：network-seek `import_comware_structured` 读 JSON 的 `parsed[命令]` dict → `_build_*_comware`
→ pydantic IR → 建图。**与旧链路 `import_comware_fixture`（re-parse raw `.txt`）产出完全等价的图元素。**

| 图元素 | 来源（`parsed` 键） | 关键字段 |
|---|---|---|
| `Device` | `display version` | `name`(回退文件名) / `vendor=h3c` / `model` / `os_version` / `serial` / `uptime_days` |
| `Interface` | `display interface brief` | `name` / `admin_status` / `oper_status` / `speed_mbps` / `duplex` / `vlan_id` |
| `VLAN` | `display current-configuration`(主) + `display vlan brief`(兜底) | `vlan_id` / `name` |
| `CONNECTS_TO` 边 | `display lldp neighbor-information list` | `src_dev-[src_intf]→dst_dev-[dst_intf]` |
| `IPSubnet` / `HAS_IP` | current-configuration 三层口 / interface | `cidr` / `gateway` |
| `ROUTES_TO` 边 | `display ip routing-table` | `dest_subnet` / `protocol` / `next_hop` / `egress_interface` |
| `VRRPGroup` / `VRRP_MASTER` | `display vrrp` | `group_id` / `role` / `virtual_ip` / `master_ip` |
| `Stack` | `display irf` | `name` / `members` |
| `MLAG` / `LAG` / `MEMBER_OF_LAG` | `display m-lag summary` + `display link-aggregation verbose` | domain / role / keepalive |
| `SecurityZone` / 接口入域 | `display security-zone` | `name` / `interfaces` |
| `SecurityPolicy` / 域间策略边 | `display security-policy ip` | `rule_id` / `source_zones` / `dest_zones` |
| `ACL` / `SecurityRule` / `VRF` | `display current-configuration` | 见解析器 |

---

## 7. 已知问题 / 约束 / 待办（截至 2026-07-22）

### ✅ 已修复（两侧已对齐）
- **P0 LLDP 解析**：由 `|` 竖线切改为**空格分列 + 列定位**，并过滤图例续行（含 `Nearest`）。
  修复前：真实链路对端端口全空 + 注入假链路到假节点 `#`/`Default`；修复后：对端端口齐全、无假边。
- **P0 VLAN brief 解析**：新增 `^\s*(\d{1,4})\s+VLAN\b` 形式，兼容真实 H3C 表格行
  （`1  VLAN 0001  GE1/0/10`）。旧逻辑命中 0 条。
- **防火墙型号**：巡检 `sync_cmdb.parse_version` 与 network-seek `parse_version_comware`
  均改为多 token，防火墙型号不再误显为 `hp_comware`（回退字面量）。
- **解析逻辑单一真源（2026-07-23 · 阶段一~三落地）**：`sync_cmdb`、`stage_c`、异常检查器、network-seek
  全部复用 `xunjian/app02/parsers/comware.py` 同一份解析；采集时一次解析落 `DeviceParseResult`、
  导出结构化 JSON、`rebuild_topology --push` 默认走 JSON 薄导入，**raw 不再被任何下游重解析**。
  历史 `CheckResult` 已 `backfill_parse_results` 全量回填（见 §7 待办末条）。

### ⚠️ 待办 / 约束
- **P1 设备命名脆弱（链路连通命门）**：真实 `display version` **无 `Device name:` 行**，
  network-seek 退回用**导出文件名**当设备名。链路能否连通完全依赖
  「`NewDevice.name` == LLDP `System Name` 完全一致」。一旦某设备改名 / 带域名 / IRF 合并名
  （如 `psw004&005...`）拆成两台独立设备，链路会 dangling。建议统一命名来源。
- **`rebuild_topology` 未实现按站点选 bolt 端口**（注释与代码不符，见第 5 节）。
- **数据质量**：个别设备 `display vlan brief` 回显串成 current-configuration（如 `oasw005`），
  需重采；解析器返回 0 是安全行为，不会脑补。
- **未巡检设备**（如知识城部分设备）无 `CheckResult` → 导出为空文件被跳过 → CMDB/图缺数据，
  需先巡检再 `rebuild_topology`。
- **历史数据已闭环（2026-07-23 阶段四）**：`DeviceParseResult` 表已建（迁移 `0006`），
  `backfill_parse_results` 已对存量 `CheckResult` 全量回填——可解析命令的
  `(device,command,time)` 组合 **100% 覆盖**（本批 258/258）。新巡检由 executor 采集时自动落库，
  无需再跑回填（解析器升级后跑一次即可刷新）。

---

## 8. 排错 / 校验清单

1. **导出是否齐全**
   ```bash
   ls cmdb_fixtures/化龙/ | head
   # 抽查某设备文件，确认 6 个 !Command: 段头齐全
   grep -c '!Command:' cmdb_fixtures/化龙/fw001.pri.2IDC4f.hualong.xc.txt
   ```
2. **巡检侧语法/健康**
   ```bash
   python manage.py check
   # CMDB 型号抽查：只剩「无数据设备」应为 hp_comware
   python manage.py shell -c "from app02.models import CmdbDevice; print(CmdbDevice.objects.filter(model='hp_comware').count())"
   ```
3. **图是否导入成功**（network-seek 侧）
   ```bash
   python -c "
   from network_seek.graph.connection import Neo4jConnection
   c=Neo4jConnection(); c.connect()
   print('Device 数:', c.run_query('MATCH (d:Device) RETURN count(d) AS n')[0]['n'])
   print('链路数:', c.run_query('MATCH ()-[r:CONNECTS_TO]->() RETURN count(r) AS n')[0]['n'])
   c.close()
   "
   ```
4. **联动断点最常见原因**：段头关键词不匹配 → 该段丢弃；`NewDevice.name` 与 LLDP 系统名不一致 → 链路 dangling；
   未巡检 → 文件为空被跳过。
