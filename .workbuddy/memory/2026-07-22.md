# 2026-07-22 工作日志（xunjian_system1）

## 一、用户自改代码核查（用户说"1、我已经改了，你检查下"）
用户在本地改了 3 处，已确认健康：
- `app02/forms.py`：删除 v1 残留 MyModel/MyForm，新增 `UploadFileForm`（Excel 上传）。
- `app02/apps.py`：启动时拉起维护守护线程（recover_running_on_startup + maintenance_loop，保留窗口默认 90 天）+ 加载 Web 编辑的 checker 覆盖。依赖 `app02/engine/maintenance.py`（已建，含 prune_old_results/recover_running_on_startup/reap_stuck_tasks）。
- `app02/migrations/0001_initial.py`：整体改写为新 schema。已验证**不破坏迁移链**——`0002~0005` 不再 CreateModel 那 8 张核心表（只建 Cmdb*/CompliancePolicy/CheckerScript 等），`makemigrations --check` 0 待生成、`showmigrations` 全 `[X]`，全新库 migrate 也不会撞表。
- DB 内 3 个 CheckerScript 覆盖（check_agg/check_logbuffer/check_system_stable）是用户 Web 合法编辑，enabled=True，有效。
- `maintenance.prune_old_results` 引用的 `DiscoveryRecord`(models.py:249)/`ComplianceResult`(models.py:307) 均存在，维护线程不会崩。

## 二、CPU/内存阈值校准（P0③，用户"1"的真意）
用户通过 Web 把 `display cpu-usage` 巡检项 `checker_config` 改成 `{'warning':40,'operator':'<'}`（含义：CPU>40% 才告警），内存 `check_memory` 的 `warning=40`。这是正确改法，但**底层正则仍是坏的**：
- 原 `parser_config.pattern = r'(\d+)%\s+in last 5 seconds'`，期望 `8% in last 5 seconds`，但 H3C V7 真实输出是 `CPU utilization in last 5 seconds:  8%`（数字在冒号后、顺序反），旧正则匹配不到 → 所有 CPU 巡检项会"解析失败"误报。
- **已修复**：seed + 线上 CheckItem 的 cpu pattern 改为 `r'5 seconds:?\s*(\d+(?:\.\d+)?)%'`，warning 同步为 40。端到端验证：`5 seconds: 8%`→解析8.0→正常；`52%`→超阈值告警；旧正则返回 None 确认已坏。
- 内存项 `check_memory` 抓 `Mem: ... 55.9%` 本来正确，warning=40 同步进 seed。

## 三、巡检后自动同步 CMDB（用户"二、帮我加钩子"）
`new_run_xunjian` 的后台 `_worker` 在巡检完成后 `call_command('sync_cmdb')`（`try/except` + `close_old_connections`），解决"巡检后 CMDB 没更新"。每次巡检结束即把最新采集解析进台账。

## 四、历史 CheckResult 含命令行清理（用户"3、帮我收尾"之一）
新增 `app02/management/commands/cleanup_cmd_lines.py`（精确剥离开头首行==命令的回声行，幂等）。已实跑：**清理 110 条**历史回显首行。新数据因 strip_command=True 本就干净。

## 五、CMDB 解析器健壮性 + 死检查器分析（用户"3、收尾" + "4、CMDB完善"）
- `sync_cmdb.parse_cpu_usage` 增强：兼容 `CPU utilization in last 5 seconds: 8%` / `CPU 5sec: 10%` / `5 seconds: 8%` 多种 H3C V7 格式。
- 死检查器分析：26 个已注册 custom checker 中，**仅 3 个未被任何 CheckItem 引用**（非之前估的 5 个）：`check_cpu`、`check_ospf_peer`、`check_security_policy_zone`。注意 `check_cpu` 没被用，是因为 CPU 巡检走内置 `threshold`+`regex` 而非自定义函数。已列出，**未删除**（等用户确认是否清理）。

## 六、未做（用户"3、没理解"=一.3 network-seek Comware 解析器校准）
已在对话中向用户解释：这是 network-seek 模块里解析真机 `display` 输出的 Comware 启发式正则，需贴真实回显才能校准，属可选项，本轮未做。

## 七、基于真实库数据自校准（用户："我已采集，你直接校准"）
用户指出命令回显已落库，无需再贴样本。直接从 CheckResult 抽真实样本校准，发现并修复多处静默错误：

1. **CPU 巡检项坏正则 → 切自定义检查器**：线上 `display cpu-usage` 仍停在 `threshold`+`regex`(pattern `5 seconds:?\s*(\d+)%`)，在 S6805 真机 `6% in last 5 seconds` 上会跨行误抓 1分钟值(4)。改为 `checker='custom'` + `checker_config={'func':'check_cpu','warning':40}` + `parser='raw'`，seed 同步改。
2. **check_cpu 隐藏 bug**：原 `re.search(r'(\d+)', line)` 在 `CPU utilization in last 5 seconds: 6%` 会抓到 "5"（5 seconds 里的5）。重写为精确匹配 `(\d+)%\s*in last 5 seconds` / `in last 5 seconds:?\s*(\d+)%` 两种 H3C V7 格式。
3. **两个 LLDP 解析器按 `|` 切（真机空格分列）→ 全错**：`sync_cmdb.parse_lldp` + `stage_c._parse_lldp` 改用空白分列，跳过图例行（含 `Nearest` 的 `* / # / Default` 说明行），并对 (local_port,peer_port,peer_device) 去重（真机同一口重复列同一对端）。
4. **parse_vlan_brief 不匹配真机**：原只认 `N  Enabled`，真机为 `1  VLAN 0001  HGE...`。加行首 `^\s*(\d+)\s+VLAN\b` 分支。
5. **parse_interface_brief 三层/二层混淆**：原把 route 块（MGE0/0/0 UP UP 192.167.0.11）当二层解析，污染 pvid/desc。改为按 `in route mode`/`in bridge mode` 分块处理。
6. **sync_cmdb IntegrityError 防御**：子表 create 循环包 `try/except IntegrityError: continue`，单条重复不拖垮整台（根因：asw001&002 的 LLDP 重复行触发 unique_together 冲突，整台静默 [FAIL]，导致 links 只存7条、vlans 存0）。

**验证**：`manage.py check` 0 issues、`makemigrations --check` 0；自校准 CPU 26/26 全对、LLDP/VLAN/IFBRIEF/VERSION 空解析全 0；`sync_cmdb` 化龙 26台 + 全量 102台 全部 [OK] 无 FAIL；asw001&002 → interfaces=145 links=39 vlans=805，LLDP 图例垃圾=0、13条带真实设备名邻居。

**已知限制（非 bug）**：
- 知识城 76 台 `CheckResult=0`（从未巡检），CMDB 空是预期，需先跑知识城巡检再由 sync 钩子自动填充。
- version 解析器实测 OK（model/os_version/uptime 都能提取），无需改。

## 八、补序列号采集闭环（display device manuinfo）
用户"继续完成未完成的任务" → 落地序列号采集（此前 serial 因未采集 manuinfo 而空）。
- **seed_inspection.py**：
  - `COMMAND_RULES` 新增 `'display device manuinfo'`（parser='raw', checker='contains'，纯采集不判错）。
  - `handle` 在 `read_site_excel` 后**自动把该命令注入每个角色的 cmds 列表**（无需用户改 Excel，重跑 seed 即生效；仅当命令在 COMMAND_RULES 才注入，保证 CheckItem 元数据正确）。
- **sync_cmdb.py**：
  - 新增 `parse_manuinfo(text)`：兼容 `Device Serial Number:` / `DEVICE SERIAL NUMBER :` / 兜底 `SN:` 三种 H3C 格式，多槽位取首个非空、去重；返回 `CmdbDevice.serial` 用的单值串。
  - `handle` 中 `manu = _latest(dev.name,'display device manuinfo')`；`serial = parse_manuinfo(...) or dv['serial']`（manuinfo 优先，display version 的 SN 兜底）。**注意顺序 bug**：初版把 `serial=... or dv['serial']` 写在 `dv=parse_version()` 之前导致 `UnboundLocalError`，已后移修复。
- **验证**：`manage.py check` 0 issues、`makemigrations --check` 0、解析器 5 种合成格式全过；端到端（给化龙一台塞合成 manuinfo → sync → serial 正确填入 `219801A0XV1099TEST`；删记录再 sync → 归零）得 E2E_OK；`sync_cmdb --site 化龙` 26 台全 [OK] 无崩。
- **待用户执行**：内网重跑 `seed_inspection --site 化龙`（及知识城）+ 跑一次巡检采集 manuinfo，CMDB 序列号即自动填充（钩子已自动触发 sync）。

## 九、整体巡检报告（项目设备整体态势页 + 导出）
用户需求：现有只有"单设备检查"，希望把库内全部数据聚合成**项目整体情况报告**。
- **新增视图** `app02/views.py`：
  - `_fleet_stats(site)`：聚合函数（页面与导出复用）。数据来源 = `NewDevice`(启用) 资产台账 + `CmdbDevice`(最新同步) 资源健康 + `AnomalyRecord`(confirm=False 累计未确认) 待处理异常 + `XunjianRecord` 最近巡检。产出：设备总数/已建台账/资源预警/待处理异常(P0+P1+P2)/健康率环/站点·角色分布/异常严重级别·站点·角色分布/资源水位预警清单/问题清单(按命令+说明聚合)/异常Top设备/最近巡检。
  - `fleet_report(request)`：渲染页（青蓝主题，复用 layout.html + .xj-card/.xj-kpi/.xj-table；资产健康用 3 段 SVG 环；分布用 CSS 条；支持 `?site=化龙/知识城` 过滤；含"导出Excel""打印"按钮）。
  - `fleet_report_export(request)`：openpyxl 导出 6 个 sheet（整体概览/分布/资源预警/问题清单/异常Top设备/最近巡检）。
- **路由** `xunjian_system1/urls.py`：`/new/report/overview/`、`/new/report/overview/export/`。
- **导航**：`app02/templates/layout.html`「结果与分析」组新增"整体巡检报告"。
- **模板**：`app02/templates/fleet_report.html`（新文件，含 @media print 隐藏侧边栏/顶栏）。
- **验证**：`manage.py check` 0 issues；直接调用视图 HTTP 200（48KB，各区块齐全）；导出 200 且 xlsx 魔数 PK 正确；`_fleet_stats('')` 实测 102 台/化龙26+知识城76/资源预警0/待处理异常133(P0=2 P1=35 P2=96)/问题78/健康率100%；`?site=化龙`→26台133异常、`?site=知识城`→76台0异常(知识城尚未巡检，符合现状)。
- **设计取舍（已向用户说明）**：异常统计采用"累计未确认=待处理"口径，与仪表盘"待确认异常"一致、与单次巡检解耦；若用户想要"仅最近一次巡检"口径，可加时间维度切换（待定）。

## 十、整体报告升级为"项目态势分析"（用户二次反馈）
用户指出第九节的报告仍是"把单台设备结果罗列/相加"，不是"对项目整体情况的分析"。本轮把后端 `_fleet_stats` 与模板 `fleet_report.html` 从"汇总罗列"升级为"整体总结分析"：

**新增分析层（_fleet_stats 返回新增字段）**：
- `grade`/`grade_label`/`grade_color`：**整体健康评级**（A优秀/B良好/C一般/D较差/E高危）。综合分 `blended = 健康分*0.6 + 覆盖度*0.4`；健康分按严重级别加权异常负担 `W=p0*4+p1*2+p2*0.5` 后 `100 - min(75, 负担/巡检设备*12)`。**硬规则：存在未处置 P0 时评级封顶 B**（避免有 P0 却评优秀）。
- `coverage_pct`/`never_synced`：**巡检覆盖度**（已建台账÷本范围设备）+ 启用但未巡检设备清单。
- `trend`/`trend_max`/`trend_first_total`/`trend_last_total`/`overall_delta`：**跨轮次趋势**——按 `XunjianRecord` 逐轮展开，叠加每轮 P0/P1/P2 构成、异常率、相对上轮变化；首末轮合计 + 整体升降。
- `failure_modes`/`fm_max`：**故障模式归因**——按巡检项(命令)聚合未确认异常，标记**系统性(影响≥3台)/局部**，按命中次数排序（根因分析）。
- `risk_by_role`/`risk_by_site`：**风险集中度**——异常设备率（有异常设备数÷该角色/站点设备数，0~100%，修正了初版用"异常条数÷设备数"导致 >100% 的 bug）。
- `narrative`/`recommendations`：**一句话总结 + 自动处置建议**（由覆盖度/P0/高频故障项/资源预警/P2积压/趋势推导）。

**模板重构** `fleet_report.html`：改为"分析在前、明细在后"六段式——
一、整体健康评级（大号评级徽章+总结+评分）；二、整体异常走势（趋势条形图+升降横幅）；三、故障模式归因表（系统性标记）；四、风险集中度（按角色/站点）；五、覆盖与缺口；六、自动建议。原 KPI/健康环/分布/资源预警/问题清单/Top设备/最近巡检降级为"附录"明细。导出 `fleet_report_export` 同步新增"异常趋势""故障模式"两张 sheet。

**验证**：`manage.py check` 0 issues；直接调用视图全/化龙/知识城均 HTTP 200、无 Traceback；评级收敛合理（全部→B 84.0 因 P0 封顶；化龙→C 65.1；知识城→A 100）；风险率回到 0~100%（化龙 100%/知识城 0%）；导出 xlsx 魔数 PK 正确（15KB）。

**用户已确认的数据现状**：CmdbDevice 现已有 102 台（知识城也已巡检同步），覆盖度 100%；待处理异常 133（P0=2/P1=35/P2=96），全部集中在化龙 26 台；高频故障项 top3 = display transceiver diagnosis interface(24台/系统性)、display logbuffer(13台/系统性)、display link-aggregation(9台/系统性)。

## 六、network-seek 联动审计（用户"检查下和network-seek联动有没有问题"）
把 network-seek 的精确解析正则套在库内真实 H3C 回显上离线验证，发现联动侧 2 个 P0 + 1 个 P1，巡检侧采集本身无碍：
- **P0① LLDP 解析器双 bug**（`network_seek/parsers/comware.py::parse_lldp_comware`）：真实 `display lldp neighbor-information list` 为空格分列，但解析器先按 `|` 切→真实链路对端端口全空(dst_intf='')；且图例续行 `# -- -- Nearest...`/`Default -- -- Nearest...` 未被 SKIP 过滤→每台设备注入 2 条假链路到假设备 `#`/`Default`（实测本台 5 条"链路"含 2 假+3 残）。**拓扑互联核心错误**。
- **P0② VLAN brief 解析器命中 0 条**：真实 `display vlan brief` 行为 `1  VLAN 0001  GE1/0/10...`，但解析器只认 `VLANs include:` 或 `^\d+ Enabled`→实测解析 0 个 VLAN；VLAN 节点只能靠 running-config 兜底、端口-VLAN 映射从 brief 彻底丢失。修正正则 `^\s*(\d{1,4})\s+VLAN` 可正确拿到 [1,100,101,110,116]。
- **P1 设备命名脆弱**：`parse_version_comware` 靠 `Device name:` 取主机名，但真实 `display version` 无此行（实测 present?=False）→退回用导出文件名当设备名，仅当"巡检 NewDevice.name == LLDP System Name 同名"约定成立才连得通；IRF 合并名 `psw004&005...` 可能 dangling。
- **验证正常**：`split_fixture_comware` 6 段段头映射 OK；`parse_interfaces_comware` 57 接口 OK；`parse_route_table_comware` 13 路由 OK；`parse_version_comware` 型号/版本/时长 OK（仅缺名）。
- **根因**：巡检系统在 B 阶段校准过 LLDP/version（改空格分列等），但**未同步到 network-seek**，两份 H3C 解析逻辑已分叉。另 `splitter.py` 的 `split_fixture` 是 NX-OS 专用、Comware 链路不用（走 `split_fixture_comware`），易误用；`rebuild_topology` 注释 bolt 7688 与默认 7687 不符。
- **待用户决策**：是否让本 Agent 直接改 network-seek 仓库修 P0①②（改动小、可离线复验），并把两边 H3C 解析逻辑统一为一份防再分叉。network-seek 是独立仓库 `C:/Users/ZSS/Desktop/network-seek`。

## 十一、network-seek 联动 P0①② 已修复并离线复验（落盘）
- 改 `C:/Users/ZSS/Desktop/network-seek/network_seek/parsers/comware.py`：
  - `parse_lldp_comware` 重写为空格分列+列定位 `(toks[0], ' '.join(toks[3:]), toks[2])`，SKIP 增 `'Nearest'` 过滤三行图例。
  - `parse_vlan_brief_comware` 增形式3 正则 `^\s*(\d{1,4})\s+VLAN\b`（兼容真实 H3C 表格行）。
- 离线复验（用化龙真实 CheckResult 回显，直接 exec 编辑后源码片段，绕过 network-seek 包导入/pydantic 依赖）：
  - LLDP：新 522 真实链路 / 0 假链路 / 0 对端端口空；旧 527 含 50 假链路 + 527 条 dst_intf 全空。
  - VLAN brief：17 台有数据设备中 16 台正确解析；oasw005 仍为 0 → 核查为其 `display vlan brief` 回显串成 current-configuration 接口段，**采集侧数据质量问题，非解析器 bug**，需返工重采。
- 语法 `ast.parse` 通过；Neo4j 端到端未跑（本环境不可达），导入建图需内网 `rebuild_topology [--push]` 复验。
- 未改：P1 命名脆弱、P2 双份逻辑分叉、`splitter.py` NX-OS 误用、rebuild_topology 端口注释不符 —— 已记入审计报告"遗留"。
- 审计报告 `network_seek_linkage_audit.md` 末新增"五、修复记录"章节。

## 十二、CMDB 防火墙型号误显 hp_comware 根因修复
- **现象**：CmdbDevice 中 20 台 FW 的 model 全为 `hp_comware`（实为 device_type 字面回退值）。
- **根因**：`sync_cmdb.parse_version` 型号正则 `H3C\s+(\S+)\s+uptime` 仅匹配单 token 型号；交换机 `H3C S6805-54HF uptime` 正常，但**防火墙型号是双 token**（`H3C SecPath M9000-X10 uptime`），正则失败 → model 空 → 回退 `dev.device_type`(=hp_comware)。
- **修复**：`sync_cmdb.py:39` 改为 `H3C\s+(.+?)\s+uptime`（非贪婪多 token）。交换机单 token 解析结果不变。
- **离线复验 + 实跑**：改后重跑 `sync_cmdb`（102 台全量）。化龙 8 台 FW → `SecPath M9000-X10`(4)/`SecPath F5000-E`(4) 正确；回退值 84→76。剩余 76 台 hp_comware 全部是**知识城未巡检、无 display version 数据**的设备（非解析 bug，待巡检后自动补齐）。
- 全库"有 H3C+uptime 但正则不匹配"设备数=0，正则通用。

## 十三、撰写联动接口文档 + network-seek 型号正则对齐
- 新增接口文档（两仓各一份，内容一致、可对称阅读）：
  - `xunjian_system1/联动接口文档_巡检与拓扑.md`
  - `network-seek/联动接口文档_巡检与拓扑.md`
  - 覆盖：系统定位、数据流、导出 fixture 格式契约（6 段命令↔段key↔解析器↔图节点映射表）、调用命令、两侧配置项、字段映射、已知问题/待办、排错清单。
- 顺带对齐：network-seek `parsers/comware.py:parse_version_comware` 型号正则 `H3C\s+(\S+)\s+uptime` → `H3C\s+(.+?)\s+uptime`（与 xunjian sync_cmdb 一致，防火墙型号不再误显），ast.parse 通过。
- 文档如实标注遗留：P1 命名脆弱（回退文件名、依赖台账名==LLDP名）、两仓解析逻辑仍建议统一、rebuild_topology 未按站点选 bolt 端口（注释与代码不符）、oasw005 vlan 数据质量问题、未巡检设备缺数据。
