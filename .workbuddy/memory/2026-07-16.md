# 2026-07-16 工作日志

## 阶段 B 方案重写（单引擎重建）
- 用户决策变更：不再"双引擎收敛"，改为**直接用单引擎、全面拥抱新巡检系统、摒弃旧逻辑**；巡检命令以知识城/化龙真实清单为准。此前已确认三选项：采集+完整规则 / 彻底删除旧引擎 / 按角色模板。
- 解析两份 Excel（`化龙/化龙配置/network_inspection/知识城设备清单_带巡检命令.xlsx`、`化龙设备清单_带巡检命令.xlsx`）：共 **102 台 hp_comware**（知识城76+化龙26），命令按设备名前缀聚成 **5 套角色模板**（FW/CSW/ASW/LSW/SRP），对应 5 个 DeviceGroup。化龙防火墙端口 8022。
- 确认新引擎数据模型与命令挂载路径：`run_xunjian` 用 `device.group.check_items`（executor L267），"按角色模板"天然成立；CheckItem 字段与 5 个 checker（threshold/contains/count/baseline/custom）接口清晰。
- 定位 executor 连接层缺口（L82-93）：缺 port/enable_password/ssh_key_file/telnet，且**未发送关分页预命令**（会 `--More--` 卡死）；对照可跑的 `inspection.py` 应连上先发 `screen-length disable`。
- 生成《阶段B_单引擎重建_实施方案与测试用例.md》：含 5 套模板完整规则配置表、NewDevice 扩字段、连接层改造、seed_inspection 幂等命令、旧引擎删除清单、实施步骤（约10~14人天）、pytest 测试用例（TC-B1~B9，覆盖模型/解析器/检查器/自定义检查器/pipeline/连接层/seed/集成/删除残留）、验收标准与风险回滚。
- 已清理本次临时脚本（_role_cmds.py/_xlsx_dump.txt 等）。MEMORY.md 已同步更新架构描述为单引擎。
- 下一步可做：B0 写 custom_checks.py + 单元测试；或 B1 NewDevice 扩字段+迁移。需在内网执行 migrate/seed。

## 阶段 B 落地 B0/B1/B2/B3（化龙/知识城分离部署）
- 用户追加要求：**化龙与知识城分开部署**，并**开始做 B0 和 B1**。实际一并完成了 B0/B1/B2/B3（代码层）。
- 重新精确提取 Excel（修正防火墙前缀 bug）：知识城 FW12/ASW58/CSW6=76；化龙 SRP1/FW8/ASW13/CSW2/LSW2=26。化龙 12 台防火墙端口 8022、其余 22。注意化龙 ASW(12命令)/CSW(17命令) 比知识城(13/18)少 `arp user-ip-conflict record`。
- **B0** `app02/custom_checks.py`：9 个自定义检查器(check_env/fan/power/device/ifbrief/agg/arp/vrrp/nqa)，@register_checker 注册；配套 `app02/tests/test_custom_checks.py`（纯 pytest 不触库）。已用 manage.py shell 验证 9 个检查器正常/异常分支 + CPU 阈值全部正确。
- **B1** `models.py` NewDevice 扩 6 字段(conn_type/port/enable_password/ssh_key_file/role/site)；生成迁移 `0014_newdevice_conn_type_newdevice_enable_password_and_more.py`。
- **B2** `executor.py` 抽出 `_build_conn_kwargs(device)`（支持 port/secret/use_keys/telnet），连接成功后发 `screen-length disable` 防 `--More--` 卡死。
- **B3** `app02/management/commands/seed_inspection.py`：幂等；全局 CheckItem（23 条命令去重，规则见 COMMAND_RULES）；按 **(站点,角色)** 建 DeviceGroup（`GRP-<站点>-<角色>`）、按站点建 CheckSet（`CS-<站点>`）；读两个 Excel 推导 role/site/连接参数导入 NewDevice，extra 写每角色默认值(down_ok/ospf_nei/bgp_nei/vrrp_master)；支持 `--site 知识城/化龙/all` 实现分别部署。
- 校验：`manage.py check` 0 issues；`makemigrations` 生成 0014；py_compile 5 个新文件无语法错误；shell 验证检查器 OK。⚠️ 开发机连不上内网 MySQL，`migrate`/`seed_inspection` 需在内网执行。
- 待做（下一步）：**B4 彻底删除旧引擎**（xunjian.py/test1.py/旧表/ConfigBackup/旧视图/旧URL/旧导入脚本+全仓 grep 校验）；内网 migrate+seed+真机联调校准 regex。

## 阶段 B4 完成：彻底删除旧引擎（2026-07-16）
- 依据《阶段B_单引擎重建_实施方案与测试用例.md》第6节删除清单，彻底删除旧巡检引擎。
- 删除内容（备份至仓库外 `C:\Users\ZSS\Desktop\广期所项目\xunjian\xunjian111\_b4_oldengine_backup\`，以保证全仓 grep 干净）：
  - 旧代码：`app02/xunjian.py`、`app02/methon/`（test1.py）、`app02/views_backup.py`、`app02/utils/migrate_old_data.py`；
  - 旧视图模块 `app02/views_01/` 下 config_backup/config_backup111/crc_query/device/device_back/fbounc/func/log_backup/log_backup1/host/upload 共 12 个文件；
  - 旧导入脚本（根目录）：`import_xunjian.py`/`import_xunjian_batch.py`/`init_new_devices.py`；
  - 旧孤立模板 30+（`xunjian.html`/`search_history.html`/`peizhiguanli_device.html`/`conf_backup_*`/`log_backup_*`/`info_xunjian.html`/`device_*`/`func_*`/`fbounc_*`/`crc_list.html`/`bound_funcgroup.html`/`boundfunc_edit.html`/`text_compare*.html`/`upload_*`/`user_list.html`/`muban.html`/`info_history*.html`/`info_list.html`/`info_edit.html`/`xunjian_result.html`），含 `layout_1.html`。
- 代码编辑：`models.py` 删 9 个旧模型 + `config_backup_upload_to`；`views.py` 删旧视图块(line 36–590)与旧导入(`from app02 import xunjian/methon`)；`xunjian_system1/urls.py` 删旧路由 token + 改 import 为 `views_01.depart/admin/account/host_xunjian`；`layout.html` 删死链接（历史记录v1/配置备份/函数管理/绑定函数组）。
- 迁移修复：0009 原模块级引用 `app02.models.config_backup_upload_to`，函数删除后触发 `AttributeError` → 改为自包含本地定义（不再依赖 models 模块）；新增 `0015_remove_configbackup_dev_and_more.py`（RemoveField configbackup.dev + 9×DeleteModel），依赖 0014。
- 校验全部通过：`manage.py check` 0 issues；`makemigrations --check --dry-run` No changes detected（幂等）；全仓 grep（排除 venv/backup/migrations）旧标识 **0 命中**（TC-B9 满足）；py_compile 全量无语法错误。⚠️ 仅 `migrations/` 历史文件仍含旧标识（必须保留，0015 为删除迁移）。
- **阶段 B 全部完成**：B0 自定义检查器 → B1 模型扩字段 → B2 连接层 → B3 seed_inspection → B4 删旧引擎。
- 待用户在内网执行：`python manage.py migrate`（含 0015 删除迁移）建表 + `python manage.py seed_inspection --site 知识城`（及 `--site 化龙`）+ 真机联调校准 regex（ospf_nei/bgp_nei/vrrp_master 默认值）。
