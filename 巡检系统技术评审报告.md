# 网络巡检系统 · 技术评审报告

> 评审对象：`xunjian_system1`（Django 4.2 + netmiko/paramiko + MySQL）
> 评审视角：金融/等保生产环境（广期所背景）
> 评审日期：2026-07-15
> 评审依据：基于实际源码审阅（`app02/engine/*`、`app02/xunjian.py`、`app02/views.py`、`app02/models.py`、`settings.py`、`auth.py`、部署文档、导入脚本）

---

## 总体评价

本项目是一套**真实可用、功能覆盖面较广**的网络巡检系统：支持多厂商设备（H3C / 华为 / Cisco / NX-OS / TAP）、命令采集、配置备份、基线对比、阈值/计数/包含/自定义四类检查、异常确认工作流、历史查询与差异化报告，并提供了一个设计良好的**新版流水线引擎**（解析器/检查器注册表 + 自定义检查器装饰器 + 并发线程池 + 差异化报告）。从"能跑起来、能出报告"的角度看，已经超过大多数原型。

但作为**金融生产/等保环境**的巡检平台，它在四个关键维度存在系统性短板：**自动化（无调度、无自动分发）、可靠性（旧代码无异常捕获、无重试、阻塞式执行）、安全性（设备凭证明文存储、密钥硬编码、不校验主机密钥）、合规性（无 TLS、无细粒度审计、无等保映射）**。此外代码库存在"新旧双引擎并存 + 单文件 75KB 巨型方法库 + 大量被注释的异常块"的维护性债务。

**结论：功能底座扎实，但距生产级/等保要求有明显差距，需要在安全、自动化、可靠性三条线上集中补课。**

---

## 各维度评分及优缺点（总分 10 分）

| 维度 | 评分 | 优点 | 缺点 |
|------|------|------|------|
| 功能完整性 | 7.0 | 覆盖命令采集、配置备份（ConfigBackup）、基线对比、阈值/计数/包含/自定义检查、异常确认、历史查询、Excel/文本批量导入；多厂商驱动。 | **缺少自动设备发现**（靠手工录入资产）；"合规检查"仅是基线文本比对，无策略化合规框架；无性能时序趋势；TAP 等专项逻辑硬编码。 |
| 架构设计 | 6.5 | 新版 `engine/` 划分清晰：`pipeline.py`（解析器/检查器注册表）、`executor.py`（并发+报告）、`reporter.py`（差异化报告），**插件化雏形好**。 | **新旧两套引擎并存**（旧 `xunjian.py`+旧表 + 新版），维护双份逻辑；`methon/test1.py` 单文件 75KB 巨型方法库；视图层承担过多职责（路由即业务）；`xunjian.py` 用 `dict` 覆盖内置名等代码异味。 |
| 自动化程度 | 4.0 | 提供一键手动巡检与批量导入脚本；新版引擎可被 API 触发。 | **完全没有任务调度**（无 celery/apscheduler/crontab）；巡检只能人工点击触发；**报告不自动分发**（无邮件/IM 推送）；执行结果不持久化为"任务"实体，无法断点续跑。 |
| 可靠性与容错 | 4.5 | 新版引擎对单设备做了 `try/except`、连接失败计入失败统计、有 `conn_timeout/read_timeout`；基线缺失有兜底。 | **旧版 `xunjian_device` 顶层 `try/except` 被注释掉**，单设备异常会经 `executor.map` 上抛导致整批 500；**无重试机制**（tenacity 已装未用）；paramiko `ConnectDevice.command` 用固定 `sleep(15/5/3)`+`recv(999999)`，大输出易截断、卡命令易挂起；无任务状态持久化与超时保护。 |
| 性能与效率 | 6.5 | 新版动态并发 `min(32, 设备数+4)`、`ThreadPoolExecutor` + `as_completed`，I/O 密集型 SSH 并发合理；新引擎 `prefetch_related` 减少查询。 | 巡检在 **HTTP 请求线程内同步跑完全部设备**，大规模（数百~上千台）会长时间占用 web worker、易被反向代理超时打断；无异步/消息队列，水平扩展受限。 |
| 安全性 | 3.5 | 登录有会话校验；管理员/用户密码经 MD5（带 SECRET_KEY 盐）存储；新版引擎对命令执行有超时。 | **设备 SSH 凭证明文存库**（`device_table.password` / `NewDevice.password` 为普通 `CharField`）；`SECRET_KEY`、数据库密码 `root123` 硬编码 `settings.py`；`DEBUG=True`、`ALLOWED_HOSTS=['*']`；paramiko `AutoAddPolicy()` **不校验 SSH 主机密钥（中间人风险）**；认证中间件 IP 白名单硬编码、且 `new_run_xunjian` 标注 `@csrf_exempt`；无强制 HTTPS/TLS。 |
| 可维护性与代码质量 | 5.5 | 新版引擎代码整洁、有 docstring、日志规范；有独立部署说明书。 | 大量**被注释掉的 `try/except` 与死代码**；`print()` 散落（`xunjian.py`）；`methon/test1.py` 巨型文件难维护；导入脚本硬编码 Windows 绝对路径（`C:\Users\ZSS\Desktop\...`）与写死时间戳；部署文档图文混用且部分截图路径为本机绝对路径（失效）；无单元测试。 |
| 扩展性 | 7.0 | 新版引擎**插件化设计优秀**：解析器/检查器注册表 + `register_checker` 装饰器注册自定义检查；巡检项/设备组/检查集模型支持模板化组合。 | 新增设备厂商需在 `_normalize_device_type` 硬编码映射；厂商覆盖有限；TAP 专项逻辑写死在 `xunjian.py` 而非可配置；旧引擎难以扩展。 |
| 报告与可视化 | 6.0 | `reporter.py` 产出 CLI + HTML 差异化报告，**"从什么变到什么"** 的 diff 摘要对网络工程师很友好；异常可确认、有历史列表。 | **无趋势图/时序可视化**（仅有列表/文本对比）；**无告警推送**（邮件/钉钉/企业微信均未实现）；报告不可订阅/定时送达；AI 辅助解析（`new_ai_parse`）依赖外部 LLM，需补充密钥管理与降级策略。 |
| 合规与标准 | 3.5 | 巡检记录含操作人（`operator`）字段，具备基础审计雏形；Django 自带密码校验器。 | 明文凭证、无 TLS、`DEBUG=True` 直接违反等保/ISO 基线；无 RBAC 细粒度权限与职责分离；无配置变更审计链（仅 notes 文本）；无等保/ISO 条款映射与合规报表。 |

---

## 综合评分

各维度加权（权重反映金融生产环境关注点，合计 1.0）：

| 维度 | 评分 | 权重 | 加权 |
|------|------|------|------|
| 功能完整性 | 7.0 | 0.12 | 0.84 |
| 架构设计 | 6.5 | 0.10 | 0.65 |
| 自动化程度 | 4.0 | 0.12 | 0.48 |
| 可靠性与容错 | 4.5 | 0.13 | 0.585 |
| 性能与效率 | 6.5 | 0.08 | 0.52 |
| 安全性 | 3.5 | 0.15 | 0.525 |
| 可维护性 | 5.5 | 0.08 | 0.44 |
| 扩展性 | 7.0 | 0.10 | 0.70 |
| 报告与可视化 | 6.0 | 0.07 | 0.42 |
| 合规与标准 | 3.5 | 0.05 | 0.175 |
| **合计** | | **1.00** | **≈ 5.34** |

- **加权总评分：5.3 / 10（百分制 ≈ 53 / 100）**
- **等级判定：不及格（生产级 / 等保视角）**
  - 说明：若仅按"功能原型"视角评价（弱化安全/合规），其功能性可得约 7.0（良好）。差距集中在安全、自动化、可靠性、合规四条高权重线上，补齐后即可进入"良好"区间。

---

## 优化方向

### 1. 短期优先优化（高价值、低工作量，建议 1~2 周）

1. **消除明文凭证**
   - 设备 SSH 密码改用 `django-fernet-fields` 或 `cryptography.Fernet` 字段级加密；`SECRET_KEY`、数据库密码从 `settings.py` 抽出到环境变量 / `.env`（不在仓库内）。
   - 评估 `python-decouple` 或 `django-environ` 管理服务配置。
2. **强制安全基线**
   - 生产 `DEBUG=False`、`ALLOWED_HOSTS` 设为具体域名/IP；反向代理层强制 HTTPS（关闭明文 80）。
   - paramiko 连接改为**校验主机密钥**（保存 known_hosts，禁用 `AutoAddPolicy`）。
   - 取消 `new_run_xunjian` 的 `@csrf_exempt`，或仅对内网 + 签名校验放开。
3. **补齐容错**
   - 恢复旧版 `xunjian_device` 的异常捕获（或统一切换到新版引擎，废弃旧路径）。
   - 在 `executor.py` 连接与单命令执行处加入 **tenacity 重试**（如连接失败重试 2 次、指数退避）；为单设备整体加超时守护。
   - 用 netmiko 的 `send_command` 替代 paramiko `sleep+recv` 的脆弱采集方式（统一到新版引擎）。
4. **清理代码债务**
   - 删除被注释的 `try/except` 与死代码；移除散落的 `print()`，统一走 `logging`。
   - 导入脚本改为参数化（路径、时间由命令行/配置传入），去掉硬编码 Windows 路径。

### 2. 中期架构改进（需一定工作量，显著提升质量，建议 1~2 月）

1. **引入调度与任务解耦**
   - 接入 **Celery + Redis（或 django-apscheduler）** 实现定时巡检；巡检任务写入"任务表"持久化（状态：排队/执行中/完成/失败），支持断点续跑与进度查询。
   - 将 `new_run_xunjian` 改为"提交任务 → 立即返回 task_id"，前端轮询/WebSocket 拉取，避免阻塞 HTTP 请求。
2. **告警与报告自动分发**
   - 实现通知抽象层：邮件（`django.core.mail` + SMTP/TLS）、企业微信/钉钉 webhook；异常/失败设备按阈值触发推送；支持定时报告订阅。
   - 报告增加**趋势可视化**（ECharts/Chart.js 接入历史 `CheckResult`，做 CPU/内存/接口错包时序图）。
3. **收敛双引擎**
   - 以新版 `engine/` 为唯一引擎，废弃旧 `xunjian.py` + 旧表（保留数据迁移脚本）；将 `methon/test1.py` 按厂商/功能拆分为模块化检查插件目录。
4. **设备发现与配置合规**
   - 增加 LLDP/CDP/ARP 自动发现与资产同步；把"合规检查"从文本基线升级为**策略规则引擎**（如"禁止明文 SNMP、要求 NTP 同步、端口安全"等可配置策略）。

### 3. 长期战略建议（平台化、智能化，建议 3~6 月+）

1. **平台化 / 自服务化**
   - 多租户 + 细粒度 RBAC（角色：巡检员/审核员/管理员），满足等保职责分离；操作全程审计日志（谁、何时、对哪台设备、执行了什么）。
2. **AI 辅助分析**
   - 把现有 `new_ai_parse` 沉淀为"异常根因建议 + 自然语言报告"能力，结合历史库做**异常趋势预测与基线自学习**（用滚动基线替代人工设定基线）。
3. **全生命周期与集成**
   - 与 CMDB / 工单系统 / ITSM 打通（异常自动开单）；配置备份纳入版本管理与回滚；对接 Zabbix/Prometheus 做"巡检+监控"一体化。
4. **合规即代码**
   - 内置等保 2.0 / ISO 27001 检查清单模板，一键生成合规差距报告，支持监管报送。

---

## 附：评审证据索引（便于复核）

- 明文凭证 / 密钥硬编码：`xunjian_system1/settings.py:22,25,27,78-87`；`app02/models.py:83,181`
- 无主机密钥校验：`app02/xunjian.py:97`（`AutoAddPolicy`）
- 旧引擎异常被注释：`app02/xunjian.py:26,83-84`
- 脆弱采集：`app02/xunjian.py:101-113`（`sleep`+`recv`）
- 无调度/告警：`app02/utils/tasks/` 为空；全局搜索 `celery/apscheduler/send_mail/钉钉` 在业务代码中无实现
- 阻塞式执行：`app02/views.py:186-211`（旧）、`app02/engine/executor.py:271-307`（新，`as_completed` 同步返回）
- 插件化设计：`app02/engine/pipeline.py:71-76,174-212`（`PARSERS`/`CHECKERS` 注册表 + `register_checker`）
- 差异化报告：`app02/engine/reporter.py:56-104,120-164`
