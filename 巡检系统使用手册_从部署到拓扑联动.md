# 巡检系统使用手册（从零部署 → CMDB → 拓扑联动）

> 适用版本：阶段 A/B/C/D 全部落地后的单引擎巡检系统（Django 4.2 + netmiko + MySQL）。
> 本文档重点讲 **各功能如何串起来用**，以及 **「每次巡检完怎么让 CMDB / 拓扑图自动跟上」**。

---

## 0. 先说结论（很多人会误解）

- **拓扑图（Neo4j）不是 AI 生成的，是确定性正则解析（ETL）。** 全程没有任何大模型 / LLM 调用。所谓「更新图」只是重跑解析器，秒级、零费用、可重复。
- **更新图 = 跑一条命令，不是重新让 AI 写一遍。** 见第 6、7 节。
- **图采用「全量重建」**：每次把旧图清空后按当前采集重新灌入。因为图是「设备当下真实状态快照」，全量比增量 diff 简单且不会脏；百台规模完全够用。

---

## 1. 系统总览与数据流（联动核心）

```
                 ┌──────────────── 一次巡检 (UI 触发) ────────────────┐
                 │                                                     │
                 ▼                                                     │
       ① executor 连设备采集 display 命令                              │
                 │  每设备每命令原始回显                                │
                 ▼                                                     │
       ② CheckResult 表（原始文本，所有功能的唯一数据源）               │
                 │                                                     │
   ┌─────────────┼──────────────────────┬─────────────────┐          │
   ▼             ▼                      ▼                 ▼          │
③ 异常引擎    ④ 阶段 C/D            ⑤ CMDB 台账          ⑥ 拓扑图     │
  (CheckItem    (设备发现/合规/         (sync_cmdb)        (Neo4j)     │
   checker)      趋势图)                                       ↑          │
                                                                     │          │
            rebuild_topology 一键串联 ⑤→⑥（见第 7 节）───────────────┘
```

**关键认知**：运行时一切以数据库里的 `CheckResult` / `CheckItem` 为真理来源。巡检系统本身只负责「采集 + 判定 + 存原始文本」；CMDB 和拓扑图都是从 `CheckResult` 二次解析出来的**派生数据**，随时可重建，丢了也不怕。

---

## 2. 从零部署（首次 / 干净库）

> 以下命令均假设位于项目根目录，使用自带虚拟环境 `./venv/Scripts/python.exe`。

### 2.1 数据库迁移（建表）
```bash
./venv/Scripts/python.exe manage.py migrate
```
- 迁移已压平为干净的 `0001_initial`，覆盖全量 schema（含 NewDevice 新字段、XunjianTask、severity、阶段 C 四表、CMDB 六表）。
- 若数据库是**全新库**才需要；若你之前已经能正常跑，说明表已建好，可跳过。

### 2.2 导入设备与检查项
```bash
./venv/Scripts/python.exe manage.py seed_inspection --site 知识城
./venv/Scripts/python.exe manage.py seed_inspection --site 化龙
```
- 从项目根目录的 `知识城设备清单_已填充命令.xlsx` / `化龙设备清单_已填充命令_v2.xlsx` 导入设备、设备分组、检查项，并刷新各检查项的 parser/checker/severity。
- **幂等**：`update_or_create`，重跑会刷新已有检查项（详见第 8 节「单向覆盖」坑）。
- 同时会建立「基础合规基线」策略（NTP 同步 / 日志主机 / VTY 超时 / Telnet 禁用 / SNMP 五条）。

### 2.3 启动服务
```bash
./venv/Scripts/python.exe manage.py runserver 0.0.0.0:8000
```
后台维护线程（reaper + 清理）会在 `runserver` 启动时自动拉起。

---

## 3. 日常巡检（采集 + 异常判定）

1. 打开「新巡检」页面 → 选择检查集 / 设备 → 触发。
2. 后端用 Django 后台线程异步执行，立即返回 `task_id`；前端轮询进度。
3. 巡检完成后：
   - **异常** 写入 `AnomalyRecord`（带 P0/P1/P2 严重级别）。
   - **原始回显** 写入 `CheckResult`（这是后续一切的来源）。
4. 在「任务中心 / 详情页」查看异常明细，失败设备可「续跑」。
5. 「验收报告」可在线查看或导出 Excel（标题动态取站点名：知识城 / 化龙）。

---

## 4. 阶段 C / D（设备发现 · 合规 · 趋势）

- **阶段 C（设备发现 + 配置合规）**：基于已采集的 `CheckResult` 二次分析（不新增连接）。
  - 设备发现：解析 `display lldp neighbor-information list`，与 `NewDevice` 比对，标记「未知邻居 / 无 LLDP 设备」。
  - 配置合规：按 `ComplianceRule`（regex / contains / absence）评估，命中写 `ComplianceResult`。
  - 入口：`new/stagecd/`、`new/stagecd/run/`。
- **阶段 D（趋势 + 报告导出）**：
  - 趋势页 `new/trend/`：CPU / 内存 / 温度历史折线图（服务端生成 SVG，无外网依赖），数据来自历次 `CheckResult`。
  - 验收报告导出 Excel：`new/report/acceptance/export/`。

---

## 5. CMDB 台账与查询页

CMDB 是「从 CheckResult 解析出来的结构化台账」，与拓扑图是**两套独立视图**（台账在 MySQL，图在 Neo4j）。

### 5.1 生成 / 刷新台账
```bash
./venv/Scripts/python.exe manage.py sync_cmdb                 # 全部站点
./venv/Scripts/python.exe manage.py sync_cmdb --site 知识城   # 单站点
```
解析以下命令进台账：
- `display version` → 设备型号 / 版本 / 序列号 / 运行时间
- `display interface brief` → 接口状态 / 速率 / 双工 / PVID / 描述
- `display lldp neighbor-information list` → LLDP 邻居链路
- `display vlan brief` → VLAN 列表
- `display current-configuration` → 三层接口 IP（Vlan-interface）
- `display cpu-usage` / `display memory` → CPU 5秒 / 内存空闲率（用于健康度判定）

每次运行**按设备重建其子表**（快照语义），所以可任意重跑。

### 5.2 查询页（侧边栏「资产与配置」分组）
- **设备列表** `/cmdb/device/`：站点下拉 + 搜索 + 健康度筛选（CPU≥80 或内存空闲<20 判告警）。
- **接口列表** `/cmdb/interface/`：站点 + 状态（UP/DOWN）+ 搜索。
- **链路列表** `/cmdb/link/`：LLDP 邻居链路。
- **IP 列表** `/cmdb/ip/`：三层接口 IP 子网。

> 注：设备列表「状态」语义 = 健康度（CPU/内存阈值），接口列表「状态」= 物理 UP/DOWN，二者含义不同，属预期。

---

## 6. 拓扑图谱（Neo4j + network-seek）

拓扑图是**厂商无关的中间表示（IR）→ Neo4j 图**，14 个 MCP 工具（路径追踪、影响面分析等）直接查图。H3C 设备的解析器（`network_seek/parsers/comware.py`）复用同一套图写入器，所以 MCP 工具零改动即可查 H3C 资产。

### 6.1 起 Neo4j
- Neo4j **Community 版不支持单实例多库**，故采用**每站点一个容器、不同 bolt 端口**：知识城 `7687` / 化龙 `7688`。
- 用 network-seek 仓库里的 `docker-compose.yml` 或自行起容器，确保 `bolt://host:端口` 可达，账号密码与 `settings.NEO4J_*` 一致。

### 6.2 导出 fixture（巡检系统侧）
```bash
./venv/Scripts/python.exe manage.py export_networkseek_fixtures
# 产出：<项目根>/cmdb_fixtures/<站点>/<设备>.txt
```
每台设备一个 `.txt`，内部按 `!Command: <命令>` 分段（版本 / 配置 / 接口 / LLDP / VLAN / 路由表）。

### 6.3 导入图（network-seek 侧）
在 network-seek 项目里：
```bash
cd C:/Users/ZSS/Desktop/network-seek
python scripts/reimport_comware.py --dir cmdb_fixtures/知识城 \
    --bolt bolt://localhost:7687 --user neo4j --pwd networkseek2024
```
脚本会 `clear_all()` 后全量重灌对应站点。化龙同理，换目录与 `7688` 端口。

### 6.4 查图（MCP）
启动 network-seek 的 MCP server 后，即可用路径追踪 / 影响面分析等工具查询 H3C 拓扑。

---

## 7. 一键刷新：rebuild_topology（重点）

把上面 5.1 + 6.2 + 6.3 三步收敛成**一条命令**：

```bash
# 仅刷新本地 CMDB 台账 + fixture（不碰 Neo4j）
./venv/Scripts/python.exe manage.py rebuild_topology
./venv/Scripts/python.exe manage.py rebuild_topology --site 知识城

# 顺带把图也刷新（需先在 settings 配好 network-seek / Neo4j）
./venv/Scripts/python.exe manage.py rebuild_topology --push
./venv/Scripts/python.exe manage.py rebuild_topology --site 化龙 --push
```

内部依次执行：
1. `sync_cmdb`（CheckResult → CMDB 表）
2. `export_networkseek_fixtures`（→ 单设备 .txt fixture）
3. `--push` 时：通过 `subprocess` 调 network-seek 的 `scripts/reimport_comware.py` 刷新 Neo4j

### 需要先配的设置（settings.py）
```python
NETWORK_SEEK_DIR = r'C:/Users/ZSS/Desktop/network-seek'        # network-seek 根目录
NETWORK_SEEK_PYTHON = r'.../network-seek/venv/Scripts/python.exe'  # 其独立 venv 解释器
NEO4J_BOLT_URI = 'bolt://localhost:7687'
NEO4J_USER = 'neo4j'
NEO4J_PASSWORD = 'networkseek2024'
```
不配 / 置空时，`rebuild_topology` 只做前两步（CMDB + fixture），不报错。

### 联动用法建议
- **每次巡检完**：跑一次 `rebuild_topology`（或 `--push`）即可让 CMDB 与拓扑图跟上最新配置与状态。
- 想完全自动化（巡检一结束就自动刷新）：可在巡检任务完成钩子里调用 `rebuild_topology` 的同两步逻辑（CMDB 侧本地即可；图侧 `--push` 需 Neo4j 在线）。当前版本未内置自动钩子，按需接入即可。

---

## 8. 新增一条巡检命令，怎么接？

「加一条巡检命令」分两种意图，接法完全不同：

### 情形 A：只想让新命令参与「异常判定」
（最常见，比如新增一条健康检测）

1. 在 `seed_inspection` 的 `COMMAND_RULES` 加一行（命令 → 检查器/阈值），或直接在界面「检查项管理」里新增 CheckItem。
2. 若是新类型判定，在 `app02/custom_checks.py` 写一个 `@register_checker` 函数。
3. 完事。**完全独立于 CMDB / 拓扑图**，不影响任何已有逻辑。

> 新命令只要进了 `CheckResult`，异常引擎就能判定它；即使你还没写解析器把它的内容塞进台账/图，也**不会报错、不会破坏链路**——只是暂不进台账/图而已。

### 情形 B：还想让新命令的内容进 CMDB / 拓扑图
（比如新增 `display ospf peer` 想把 OSPF 邻居拓扑进图）

需要**加性挂载**解析器，挂 4 个点（每点都是「加函数 + 注册」，不动已有解析器）：

| 步骤 | 文件 | 改动 |
|---|---|---|
| ① 采集 | `seed_inspection` COMMAND_RULES | 加该命令（让它进 CheckResult） |
| ② 进台账 | `app02/management/commands/sync_cmdb.py` | 加 `_latest(dev, '命令')` + 调对应 `parse_*` 写表 |
| ③ 进 fixture | `app02/management/commands/export_networkseek_fixtures.py` 的 `SECTIONS` | 加一行 `('display xxx', 'display xxx')` |
| ④ 进图 | `network_seek/parsers/comware.py` | 写 `parse_xxx_comware` + 在 `_cmd_to_key` 登记 + 在 `importer.import_comware_fixture` 调 `build_*` |

**示例：加 `display vlan brief` 进图**（本手册对应版本已内置该修复）：
- ② `sync_cmdb` 早已解析它进 `CmdbVlan`；
- ③ `export_networkseek_fixtures` 的 `SECTIONS` 已加入 `display vlan brief`；
- ④ `comware._cmd_to_key` 把 `vlan` 映射到 `display_vlan_brief`，`importer` 用 `parse_vlan_brief_comware` 兜底建 VLAN 节点（running-config 已建过的跳过，避免覆盖名字）。

> **VLAN 一致性说明**：图里的 VLAN 节点原本就靠 `display current-configuration` 的 `vlan N` 定义进来；本次修复额外让专门的 `display vlan brief` 命令也流进图，作为兜底，保证「台账有 VLAN、图里也一定有」。

### ⚠️ 单向覆盖坑（务必记住）
- `seed_inspection` 用的是 `update_or_create`：**Python 的 COMMAND_RULES 会覆盖数据库里的 CheckItem**。
- 所以「日常改检查项」请走**界面编辑**（改完即生效，不碰 seed）；只有「改了 COMMAND_RULES 想批量同步」才跑 seed。
- **不要**先界面手改某条、又跑 seed，否则界面改的会被冲掉。两种工作流二选一，别混改同一条。

---

## 9. 典型联动场景

**场景 1：日常巡检后出 CMDB + 拓扑**
> 触发巡检 → `rebuild_topology --push` → CMDB 四列表刷新、Neo4j 图刷新 → 在 CMDB 页看设备健康度，在 MCP 里做路径追踪。

**场景 2：新增一条 OSPF 邻居健康检查并拓扑化**
> 情形 A 加 CheckItem+checker 做异常判定 → 情形 B 挂 4 点解析器把 OSPF 邻居进图 → 重跑 `rebuild_topology --push` → 影响面分析能画出 OSPF 邻居关系。

**场景 3：配置变更复盘**
> 巡检 N 次后，`趋势页`看某设备 CPU/内存曲线；`阶段 C`看合规是否漂移；`验收报告`出阶段性结论。

---

## 10. 常见问题

- **Q：每次都要重新让 AI 生成图吗？**
  A：不需要。图是确定性正则解析重建，重跑命令即可，无任何 AI 调用。
- **Q：图会越攒越脏吗？**
  A：不会。`rebuild_topology --push` / `reimport_comware.py` 都是 `clear_all()` 后全量重灌。
- **Q：旧 CheckResult 太多怎么办？**
  A：`CHECKRESULT_RETENTION_DAYS=90`，后台维护线程自动清理超期记录；也可 `python manage.py prune_results --days 90` 手动清。
- **Q：任务卡在 running 不动？**
  A：维护线程冷启动会回收卡死任务（running→failed），重跑即可。
- **Q：VLAN 在台账有、图里没有？**
  A：本版本已修复——`display vlan brief` 现在也会流进图（兜底建节点）。若仍缺，检查 fixture 里是否含 `!Command: display vlan brief` 段。
```
