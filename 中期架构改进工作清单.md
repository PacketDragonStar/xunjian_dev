# 网络巡检系统 · 中期架构改进工作清单

> 范围约束（本清单已排除）：
> - ❌ 安全方向（密钥/凭证加密、HTTPS、主机密钥校验等）—— 内网无外网，暂缓
> - ❌ 告警与消息推送（邮件/企业微信/钉钉/IM）—— 本期不做
> - ✅ 仅聚焦：调度解耦、双引擎收敛、设备发现与合规、报告趋势可视化
> 建议总周期：1~2 个月（按单人估算，见每项 T 恤尺寸）

---

## 阶段 A — 任务调度与解耦（最高优先，先行）

> 目标：巡检不再阻塞 HTTP 请求；可定时、可追踪、可续跑。

- [ ] **A1. 引入异步任务层**
  - 选型：`Celery + Redis`（推荐，解耦最彻底）或 `django-apscheduler`（轻量、无额外组件）
  - 产出：独立 worker 进程，与 web 进程分离
  - 工作量：M
- [ ] **A2. 设计巡检任务表 `XunjianTask`**
  - 字段建议：`task_id`(uuid)、`status`(排队/执行中/完成/失败/部分失败)、`operator`、`scope_type`(checkset/device)、`scope_ids`、`total`、`done`、`failed`、`created_at`、`started_at`、`finished_at`、`summary`
  - 加索引：`(status, created_at)`；状态机用枚举约束
  - 工作量：S
- [ ] **A3. 改造 `new_run_xunjian`（`app02/views.py:600`）**
  - 改为"提交即返回 `task_id`"，巡检逻辑移入 Celery task
  - `engine/executor.py` 的 `run_xunjian` 增加 `task_id` 参数，逐设备完成时回写进度（`done/failed`）
  - 工作量：M
- [ ] **A4. 前端任务中心**
  - 新增"任务列表/详情"页：显示状态、进度条、起止时间、失败设备清单
  - 前端轮询（或 WebSocket）拉取进度；历史记录关联到 `task_id`
  - 工作量：M
- [ ] **A5. 断点续跑**
  - 任务失败后可"仅重跑失败设备"：以 `XunjianTask.failed_device_ids` 为输入二次提交
  - 工作量：S

**阶段 A 验收**：点击巡检后页面秒回；任务列表可见实时进度；worker 重启不丢任务；失败设备可单选重跑。

---

## 阶段 B — 收敛双引擎（降低长期维护债务）

> 目标：以新版 `app02/engine/` 为唯一引擎，废掉旧 `xunjian.py` + 旧表，拆分巨型方法库。

- [ ] **B1. 覆盖度核对**
  - 列出旧引擎（`info_xunjian` / `xunjian_device` / `methon/test1.py`）支持的厂商、命令、检查项
  - 对照新版 `CheckItem`/检查器注册表，确认新版缺失项并补齐
  - 工作量：S
- [ ] **B2. 旧→新数据迁移脚本**
  - `device_table` → `NewDevice`；`function_table` → `CheckItem`；旧 `result_*` → 新版记录表
  - 双写验证后再切读，保留回滚方案
  - 工作量：M
- [ ] **B3. 废弃旧引擎入口**
  - 下线 `views.py` 中 `info_xunjian` / `info_xunjiantest` 及 `xunjian.py` 调用；统一走 `engine/`
  - 旧表标记为只读/归档，确认无人引用后删除
  - 工作量：S
- [ ] **B4. 拆分 `methon/test1.py`（75KB 巨型文件）**
  - 按厂商/功能拆为模块目录：`methon/h3c/`、`methon/huawei/`、`methon/cisco/`、`methon/common/`
  - 通过现有 `register_checker` / 导入机制自动加载，消除硬编码映射
  - 工作量：L
- [ ] **B5. 代码债务清理**
  - 删除被注释的 `try/except`（`xunjian.py:26,83`）、散落 `print()`、死代码
  - 统一日志到 `logging`，命名规范化（去掉 `dict` 覆盖内置名等）
  - 工作量：S

**阶段 B 验收**：仓库内仅一套引擎；`test1.py` 消失；全量巡检结果与迁移前一致；无 `print` 噪音。

---

## 阶段 C — 设备发现与配置合规（功能深化）

> 目标：从"手工录资产 + 文本基线"升级为"自动发现 + 策略化合规"。

- [ ] **C1. LLDP/CDP/ARP 自动发现**
  - 新增采集项：`show lldp neighbors` / `show cdp neighbors` / `show arp`
  - 用 TextFSM 解析为结构化邻居清单（设备名、IP、端口、平台）
  - 工作量：M
- [ ] **C2. 资产同步**
  - 发现结果生成"建议资产清单"，可一键去重写入 `NewDevice` / `DeviceGroup`
  - 工作量：S
- [ ] **C3. 合规策略规则引擎**
  - 在现有 `CHECKERS` 注册表上扩展专用 checker（如：禁止明文 SNMP、要求 NTP 同步、端口安全启用、ACL 基线一致等）
  - 策略以 `CheckItem` 形式配置，可绑定设备组，复用现有执行/报告链路
  - 工作量：L
- [ ] **C4. 合规报表**
  - 按策略输出"通过/不通过"清单 + 整改建议（复用 `reporter.py` 差异化报告能力）
  - 工作量：M

**阶段 C 验收**：新设备可通过邻居发现自动入册；可配置一条合规策略并跑出通过率报表。

---

## 阶段 D — 报告趋势可视化（非告警，可选后置）

> 说明：仅做"看得到趋势"，不做任何主动推送/告警。可放在 A/B/C 之后。

- [ ] **D1. 历史时序图**
  - 将 `CheckResult` 中阈值类指标（CPU/内存/接口错包/温度）接入 ECharts/Chart.js
  - 按设备 + 时间段绘制趋势曲线
  - 工作量：M
- [ ] **D2. 基线漂移可视化**
  - 当前值 vs 基线值 diff 时间线（复用 `AnomalyRecord.baseline_val/current_val`）
  - 工作量：S
- [ ] **D3. 报告导出**
  - 支持 HTML/PDF 导出，可按检查集 + 时间段筛选
  - 工作量：S

**阶段 D 验收**：历史页可切换"列表/趋势图"；可导出某次或某时间段的报告。

---

## 推荐执行顺序与依赖

```
A（调度解耦）  ──►  B（引擎收敛）  ──►  C（发现+合规）  ──►  D（趋势可视化，可选）
   │                │
   └─ A 不依赖 B；B 建议在 A 之后（先有任务表，迁移过程可追踪）
   └─ C3 复用 B 已稳定的 checker 注册表，故 C 放 B 后
   └─ D 独立，可并行于 C 之后或随时插入
```

## 明确不在本期范围（按你的要求）

- 安全：凭证加密、密钥/DB 密码环境变量化、`DEBUG=False`、主机密钥校验、HTTPS、CSRF 收紧
- 告警/通知：邮件、企业微信、钉钉、Webhook、定时报告订阅
- 长期平台化（多租户 RBAC、ITSM 对接、AI 根因、等保报表）留待后续阶段

## 风险与注意

1. **B 阶段数据迁移是最大风险点**：务必先双写验证、保留回滚，迁移期间新旧表并存。
2. **A 阶段 Celery 引入 Redis** 会增加一个内部组件；若不想引入中间件，`django-apscheduler` 更轻，但调度可靠性弱于 Celery（+Redis 的持久化）。
3. **C3 策略引擎** 不要另起炉灶，尽量复用现有 `CHECKERS` 注册表，避免第三套执行逻辑。
