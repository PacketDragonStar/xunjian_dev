# 解析器单一真源 + 采集时一次解析 —— 详细设计文档

> **状态**：✅ 阶段一 pilot 已落地（2026-07-23）。✅ **阶段二已落地（2026-07-23）**：`DeviceParseResult` 模型 + 迁移 `0006` + 采集时一次解析落库 + sync_cmdb/检查器消费结构化（带回退）。✅ **阶段三已落地（2026-07-23）**：`export_networkseek_fixtures` 默认产结构化 JSON（`<device>.json`，含 `schema_version` + `parsed` 命令→规范化 dict）；network-seek 新增 `import_comware_structured` / `full_reset_and_import_comware_json` 薄导入，直接消费结构化 dict 建图、**不再 re-parse raw**；`rebuild_topology --push` 默认走 JSON，旧 `.txt` 路径保留为 `--legacy-fixture` / `reimport_comware.py --no-json` 回退。✅ **阶段四已落地（2026-07-23）**：《联动接口文档》导出契约章节已更新（JSON 为主契约、`.txt` 为回退，引用本文 §4 schema）；`migrate` 建表后 `backfill_parse_results` 对存量 `CheckResult` 全量回填，可解析命令组合 **100% 覆盖（258/258）**，历史数据闭环。
> **适用仓库**：`xunjian_system1`（基座，拥有解析真源）、`network-seek`（可选附加，消费结构化产物）
> **关联文档**：`联动接口文档_巡检与拓扑.md`
> **决策来源**：用户拍板 —— ①解析真源内置 xunjian（`app02/parsers/`）；②采集时一次解析落库；③注册粒度仅按 OS 家族（hp_comware）；④ **network-seek 引用 xunjian 解析包（方案 A）**，且解析执行分**双通道**（巡检内轻量 / 控制面按需），避免巡检变慢。

---

## 1. 背景与问题

同一份设备命令回显（如 `display version` / `display lldp neighbor-information list`）当前被**解析两遍、且由两套互相独立、已分叉的代码**处理：

| 阶段 | 代码位置 | 干了什么 |
|---|---|---|
| 第一遍（xunjian） | `app02/management/commands/sync_cmdb.py` 的 `parse_version`/`parse_lldp`/`parse_vlan_brief`/`parse_interface_brief`/`parse_manuinfo`/`parse_cpu_usage`/`parse_memory_free` | 解析 raw → `CmdbDevice` |
| 第一遍（xunjian） | `app02/engine/stage_c.py:_parse_lldp` + `app02/custom_checks.py` 内联正则 | 解析 raw → 异常判定 `AnomalyRecord` |
| 第二遍（network-seek） | `network_seek/parsers/comware.py` 的 `parse_version_comware`/`parse_lldp_comware`/`parse_vlan_brief_comware`/… | 再解析 raw → Neo4j 图 |

**后果**（前几轮已实锤并各修一遍）：
- 两套解析逻辑**已分叉**：LLDP 假链路、VLAN 命中 0、防火墙型号误显，xunjian 改完 network-seek 又得改一遍。
- raw 被重复解析，无型号级复用机制 —— 换台同型号交换机仍走同一段脆弱正则，新型号就得重写。

这正是用户感知到的"处理太频繁、且同型号应复用脚本"。

---

## 2. 目标与非目标

**目标**
- **G1 单一真源**：所有 Comware 语义解析逻辑只存在一份，位于 `xunjian/app02/parsers/`。
- **G2 一次解析**：raw 在采集返回时解析一次，结构化结果持久化；CMDB 同步、异常判定、topology 导入均消费该结构化数据，raw 不再被下游重解析。
- **G3 型号复用**：同 OS 家族（hp_comware）设备自动命中同一解析器；新型号/新格式变体只改一处。
- **G4 解耦**：network-seek 不部署时 xunjian 完全不受影响；两仓通过 versioned JSON 契约解耦。

**非目标（明确不做）**
- 不引入消息队列 / 流处理。
- 不做跨厂商（仅 hp_comware；家族内格式变体在解析器内部回退处理）。
- 不改 Neo4j 图模型本身（pydantic IR、图构建保留，仅替换"raw→IR"这一段）。

> **重要厘清**：`app02/engine/pipeline.py` 里的 `PARSERS`（`raw`/`regex`/`strip_ts`/`textfsm`）是**命令输出后处理层**（去 prompt、正则抽字段供检查器用），不是设备型号语义解析器。**本方案的"单一真源"专指后者**，前者维持现状不动，以保证 CheckItem 配置灵活性。

---

## 3. 总体架构

```
                采集命令
                   │
                   ▼  netmiko/paramiko 返回 raw 文本
        ┌──────────────────────────────┐
        │  app02/engine/executor (采集) │
        └──────────────────────────────┘
                   │ raw
                   ▼  ★ 仅在此处解析一次
        ┌──────────────────────────────┐
        │  app02/parsers/  (单一真源)    │  @register_parser(family='hp_comware')
        │  get_parser().parse(raw)      │  → 规范化结构化 dict
        └──────────────────────────────┘
                   │ 结构化 JSON
                   ▼
        ┌──────────────────────────────┐
        │  DeviceParseResult (落库)      │  (device, command, collected_at, data=JSON)
        │  raw 原文仍留 CheckResult      │
        └──────────────────────────────┘
           │              │               │
           ▼              ▼               ▼
      sync_cmdb      异常检查器       export_networkseek_fixtures
           │              │               │ 产 结构化 JSON 文件
           ▼              ▼               ▼
      CmdbDevice    AnomalyRecord      (契约 schema)
                                      │
                          network-seek（可选插件）
                                      ▼
                            JSON → Neo4j 薄导入（不再 re-parse raw）
```

> **解析执行双通道（回应"巡检会不会变慢"的顾虑）**：单一真源指的是**解析代码只存在一份**（在 `xunjian/app02/parsers/`），但**解析的执行时机分两路**，互不拖累：
> - **通道一 · 巡检内（始终执行，轻量）**：仅解析 CMDB + 异常判定所需的"核心命令"子集（version / interface / lldp / vlan / cpu / memory / logbuffer 等）—— 与今天巡检所做的解析量**完全一致**，不新增控制面重解析，**巡检耗时不变**。
> - **通道二 · 按需（network-seek 触发，可选）**：当用户构建拓扑 / 控制面视图时，由 network-seek 调用**同一套** `app02/parsers/` 解析函数，针对控制面命令（ospf neighbor / bgp summary / route-policy …）做解析。只解析被分析的设备、且仅在需要时执行 → 这就是"解析另外做接口、有需要时手动解析更新"，但复用同一份代码、不再分叉。
>
> 关键点：控制面命令的**采集**也必须在 xunjian 侧发生（network-seek 只消费、不采集）。因此命令集需打 `collection_scope` 标签：核心命令巡检时始终采集；控制面/拓扑命令标 `scope='topology'`，仅在触发拓扑构建时按需采集（见 §7）。

---

## 4. 规范化结构化 Schema（核心契约）

`DeviceParseResult.data` 的逐命令结构（两仓共同依赖的契约）：

```jsonc
// display version
{ "model": "S6805-54HF", "os_version": "7.1.070", "uptime_days": 312 }
// display device manuinfo
{ "serial": "210235A... " }
// display interface brief
{ "interfaces": [ { "name": "GE1/0/1", "status": "up", "protocol": "up" } ] }
// display lldp neighbor-information list
{ "links": [ { "src_intf": "GE1/0/1", "dst_device": "psw004...", "dst_intf": "GE1/0/24" } ] }
// display vlan brief
{ "vlans": [ { "id": 1, "name": "default" }, { "id": 100, "name": "" } ] }
// display ip routing-table
{ "routes": [ { "dest": "0.0.0.0/0", "proto": "Static", "nexthop": "10.0.0.1", "out_intf": "GE1/0/1" } ] }
// display current-configuration
{ "sysname": "oasw005...", "vlans": [...], "interface_vlans": [...], "ips": [...], "acls": [...], "vrfs": [...] }
// display cpu-usage / display memory
{ "cpu_usage": 8, "mem_free_pct": 73 }
// display environment                       （风扇 / 电源 / 温度）
{ "environment": { "temp": 45, "fans": [ { "id": 1, "status": "ok" } ], "powers": [ { "id": 1, "status": "ok" } ] } }
// display transceiver diagnosis interface   （光模块，v2 Ticket 3 已有 IR）
{ "transceivers": [ { "intf": "GE1/0/1", "type": "10G-SR", "tx_power": -2.1, "rx_power": -3.4, "temp": 41, "alarm": false } ] }
// display ip ospf neighbor                  （v2 · 控制面 Ticket 16 → OSPFPeer 边）
{ "ospf_peers": [ { "neighbor_ip": "10.0.0.2", "area": "0.0.0.0", "state": "Full",
                    "interface": "GE1/0/1", "cost": 10, "network_type": "broadcast" } ] }
// display bgp peer / display bgp summary     （v2 · 控制面 Ticket 17 → BGPPeer 边）
{ "bgp_peers": [ { "remote_device": "core01", "remote_as": 65001, "local_as": 65001,
                   "peer_type": "ibgp", "session_state": "Established" } ] }
// display route-policy / current-config 段   （v2 · PBR / 策略路由 → RoutePolicy 节点）
{ "route_policies": [ { "name": "RP-EXPORT", "nodes": [ { "seq": 10, "action": "permit", "match": "...", "apply": "..." } ] } ] }
// display ip routing-table 扩展             （备份路径计算所需，v2 Ticket 19 → ECMP/AD/LFA）
{ "routes": [ { "dest": "10.0.0.0/24", "proto": "OSPF", "nexthop": "10.0.0.2",
                "out_intf": "GE1/0/1", "ad": 110, "cost": 20, "ecmp": true } ] }
// display vrrp / display tunnel / display vxlan / display ospf vlink （v2 逻辑层节点）
{ "vrrps": [...], "tunnels": [...], "vxlan": [...], "virtual_links": [...] }
```

- **字段取舍原则**：以 CMDB 与 Neo4j 两侧实际消费的并集为准。当前已覆盖 CMDB（model/os/uptime/serial/cpu/mem/interface 状态/environment/transceiver）+ Neo4j 物理层（links/vlans/routes/sysname）；**v2 控制面扩展**（ospf_peers/bgp_peers/route_policies/routes 的 ad·cost·ecmp/tunnels/vxlan/virtual_links）对齐自 `network-seek/BOARD.md` v2 候选 Ticket 16/17/19/22 + `ir/logical.py` 节点定义。其中"备份路径"对应 Ticket 19（ECMP 枚举 + AD 回退 + SPF/LFA 备用路径计算），依赖 `routes` 中的 `ad`/`cost`/`ecmp` 字段。
- **版本化**：schema 带 `schema_version` 字段，便于 network-seek 侧做兼容判断。

---

## 5. 模块布局（xunjian 内新建 `app02/parsers/`）

```
app02/parsers/
  __init__.py      # 暴露 parse_device_command(device, command, raw) -> dict
  registry.py      # REGISTRY + @register_parser + get_parser（查找链+回退）
  base.py          # ParserBase 抽象 / 规范化 dict 辅助
  comware.py       # 合并后的 Comware 语义解析器（唯一真源）
```

**待合并的现有代码（改为调用 comware.py，原处删除或留薄壳）**：
- `sync_cmdb.py`：`parse_version`(L34) / `parse_interface_brief`(L63) / `parse_lldp`(L121) / `parse_vlan_brief`(L156) / `parse_ips_from_config`(L177) / `parse_cpu_usage`(L202) / `parse_memory_free`(L227) / `parse_manuinfo`(L235)
- `stage_c.py`：`_parse_lldp`(L20) → 直接复用 `comware.parse_lldp`
- `custom_checks.py`：fan/power/device/env 等文本存在性检查可保留 raw 文本判定；ifbrief/agg/arp/vrrp/nqa 中涉及结构化的，改收 `comware` 结构化输出

---

## 6. 注册表设计

```python
# registry.py
REGISTRY = {}  # key: (vendor, family, category, model) -> fn

def register_parser(vendor, family, category=None, model=None):
    def deco(fn):
        REGISTRY[(vendor, family, category, model)] = fn
        return fn
    return deco

def get_parser(vendor='hp', family='comware', category=None, model=None):
    # 查找链：精确 model → category → family → 通用兜底
    for key in [(vendor,family,category,model),
                (vendor,family,category,None),
                (vendor,family,None,None)]:
        if key in REGISTRY:
            return REGISTRY[key]
    raise NotImplementedError(...)
```

```python
# comware.py —— 仅按 OS 家族注册一个通用解析器
@register_parser(vendor='hp', family='comware')
def parse_version(text): ...        # 内部已兼容 SecPath M9000-X10 多 token（防火墙变体回退）

@register_parser(vendor='hp', family='comware')
def parse_lldp(text): ...           # 空格分列 + 图例跳过（前轮已修）

@register_parser(vendor='hp', family='comware')
def parse_vlan_brief(text): ...     # 兼容 VLANs include / ^\d+ Enabled / ^\d+ VLAN 表
# ... interface / route / running_config / manuinfo / cpu / memory 同理
```

- **"仅按 OS 家族"的落地方式**：所有 hp_comware 设备只命中 `family='comware'` 这一个解析器；防火墙/交换机等格式差异在该解析器**内部做变体回退**（如 version 正则已兼容双 token），**不**作为独立注册项。新增型号若格式特殊，只需在该函数内补一个分支，仍是一份代码。
- `parse_device_command(device, command, raw)` 按 `device.device_type` / `role` 推导 `(vendor, family, category)`，调 `get_parser` 后分发到对应 `parse_<command>`。

---

## 7. 数据流（采集时一次解析）

1. `executor` 命令返回 `raw` → 存入 `CheckResult.result`（保留原文可追溯）。
2. `pipeline` 在采集后调 `parse_device_command(device, command, raw)` → 结构化 dict。
3. 结构化 dict 存入 **`DeviceParseResult`**（新增模型，见 §9.1）。
4. 下游消费者（sync_cmdb / 异常检查器 / export）**只读 `DeviceParseResult.data`**，不再碰 `CheckResult.result`。

> **向后兼容**：在 `DeviceParseResult` 尚未生成（历史数据 / 未跑回填）时，`sync_cmdb` 等应**自动回退到实时调 `get_parser().parse(raw)`**，保证阶段切换期不中断；回填命令跑完后回退路径可摘掉。

> **命令采集范围（`collection_scope`）**：控制面 / 拓扑命令（ospf neighbor、bgp summary、route-policy、tunnel、vxlan 等）**不打入常规巡检命令集**，而是标 `scope='topology'`，由 `rebuild_topology`（或专用 `collect_control_plane` 命令）按需向设备采集后再解析。巡检默认命令集维持现状、耗时不变；控制面采集/解析只在用户主动触发拓扑/控制面视图时发生（通道二）。

---

## 8. 各仓改动点

### 8.1 xunjian（基座）

| 改动 | 说明 |
|---|---|
| 新增 `app02/parsers/` 包 | 单一真源（registry + comware 合并解析器） |
| 新增模型 `DeviceParseResult` | `(device FK, command, collected_at, schema_version, data JSON)` + 迁移文件 |
| `pipeline.py` / `executor` | 采集后触发 parse-once，写 `DeviceParseResult` |
| `sync_cmdb.py` | 删除自有 `parse_*`，改读 `DeviceParseResult.data`（无则实时 parse 回退） |
| `stage_c.py` / `custom_checks.py` | `_parse_lldp` 等改调 `comware`；结构化检查器消费 `data` |
| `export_networkseek_fixtures.py` | 不再写 raw `.txt`，改写**结构化 JSON 文件**（§4 契约） |
| 新增 `backfill_parse_results` 命令 | 把历史 `CheckResult` raw 补解析成 `DeviceParseResult` |

### 8.2 network-seek（可选插件）

| 改动 | 说明 |
|---|---|
| 新增 JSON 导入器 | 读 xunjian 结构化 JSON → map 到现有 pydantic IR → 建图（替换 `reimport_comware` 的 raw 重解析段） |
| 退役 raw 重解析 | `comware.py` 的 `split_fixture_comware` + `parse_*_comware` 不再被主导入链路调用（可保留为工具函数，但不在 `full_reset_and_import_comware` 路径上） |
| 依赖方式（已确认：**方案 A**） | network-seek 以 git submodule / 复制锁定版引用 `xunjian/app02/parsers`，直接复用同一套解析函数（控制面命令也走通道二按需调用）。不再各自维护一份 raw 解析代码。 |

> **已确认采用方案 A（引用 xunjian 解析包）**：用户明确选择 A，避免双份契约维护成本。但为隔离风险，network-seek 应锁定 `xunjian/app02/parsers/` 的**某个提交/版本**（submodule pin 或复制快照），xunjian 侧解析器升级时显式 bump。解析**执行**仍走 §3 双通道：巡检只跑核心子集；控制面由 network-seek 按需调用同一 `get_parser()`（通道二）。两仓通过 §4 的 versioned JSON 契约 + 共享代码双重对齐。xunjian 仍不引用 network-seek 任何代码（单向依赖）。

---

## 9. 迁移与回退（分阶段、可回退）

| 阶段 | 内容 | 回退方式 |
|---|---|---|
| **阶段一（pilot）** | 建 `app02/parsers/`，合并 Comware 解析器；`sync_cmdb` 改调它。CMDB 输出须**逐字段 diff 一致**。 | 不动采集；旧 `parse_*` 暂留作对照，异常即还原调用。 |
| **阶段二** | ✅ `pipeline` 采集后 parse-once 落 `DeviceParseResult`；`sync_cmdb` 消费结构化（实时 parse 回退）；`stage_c` 复用单一真源；`check_agg`/`check_vlan`/`check_zone` 消费结构化（raw 回退）；`backfill_parse_results` 回填历史。 | 保留 raw；`sync_cmdb`/检查器带"无 DeviceParseResult 则实时 parse"回退开关（已落地）。 |
| **阶段三** | ✅ `export` 产结构化 JSON；network-seek 加 JSON→Neo4j 薄导入，退役 raw 重解析。 | export 保留 `--raw` `.txt` 导出开关；network-seek 旧导入器（`import_comware_fixture`/`reimport_comware.py --no-json`）保留备用。 |
| **阶段四** | ✅ 更新《联动接口文档》导出契约章节（§3 改为 JSON 为主、`.txt` 为回退，引用本文 §4 schema；§6.2 改结构化导入路径；§7 收敛状态标 ✅）；跑 `backfill_parse_results` 补历史（迁移 `0006` 建表后，258/258 可解析组合 100% 覆盖）。 | 历史数据可随时重跑回填。 |

---

## 10. 风险与缓解

- **解析行为差异**：阶段一用 CMDB 输出逐字段 diff 校验，确保合并后零行为变化。
- **采集时性能**：parse-once 增加采集期 CPU，但省去后续 N 次重解析，净收益；且解析在后台线程，不阻塞 UI（阶段 A 已异步）。
- **历史数据缺失**：`backfill_parse_results` 命令覆盖；回退路径保底。
- **契约漂移**：JSON schema 带 `schema_version`，network-seek 侧做版本兼容。
- **防火墙变体**：已在 `family='comware'` 解析器内部回退处理，不破坏"仅按家族"原则。

---

## 11. 验收标准

- **阶段一**：`sync_cmdb` 前后 `CmdbDevice` 全字段一致（化龙 26 + 全量 102 抽查）；`manage.py check` 0 issues。
- **阶段二**：同一份 `CheckResult` 只被解析一次（新增 `DeviceParseResult` 有记录）；异常判定结果与改造前一致。
- **阶段三**：network-seek 导入后 Neo4j 节点/边数与改造前等价（拓扑无假链路、VLAN 不丢）；导出文件为结构化 JSON。
- **阶段四**：✅ 接口文档契约章节更新（JSON 为主契约）；历史 `CheckResult` 回填完成、`DeviceParseResult` 对可解析命令 100% 覆盖（258/258，2026-07-23 实测）。

---

## 12. 开放问题（评审结论）

1. **network-seek 依赖方式** → **已确认：方案 A**。network-seek 引用 `xunjian/app02/parsers/`（锁定版本），直接复用解析函数；解析执行走 §3 双通道（巡检轻量 / 控制面按需），巡检耗时不变。
2. **raw `.txt` 导出** → **已确认：保留**。阶段三 export 仍提供 raw `.txt` 调试开关（与结构化 JSON 并行产出），便于排错与回退。
3. **`DeviceParseResult` 落库形态** → **已确认：独立模型**。理由见下（与 `CheckResult` 扩字段的本质区别）。
4. **schema 字段范围** → **已扩展**：§4 补齐 v2 控制面字段（ospf_peers / bgp_peers / route_policies / routes 的 ad·cost·ecmp / tunnels / vxlan / virtual_links / environment / transceivers），对齐 `network-seek/BOARD.md` v2 候选（Ticket 16/17/19/22）与 `ir/logical.py`。"备份路径"= Ticket 19（ECMP + AD 回退 + SPF/LFA），依赖 `routes.ad/cost/ecmp`。

### 12.1 附：`DeviceParseResult` 独立模型 vs `CheckResult` 扩 JSON 字段（Q3 解答）

| 维度 | 方案 A：独立模型 `DeviceParseResult(device, command, collected_at, schema_version, data JSON)` | 方案 B：`CheckResult` 加 JSONField |
|---|---|---|
| 数据性质 | 解析结果是"设备×命令的**当前态**"，访问模式是"按设备+命令取最新一份" | `CheckResult` 是**巡检日志**（每次巡检一行，随运行数膨胀），访问模式是"按时间查历史" |
| 查询 | `DeviceParseResult.objects.filter(device, command).latest()` 干净直接 | 需 `CheckResult.objects.filter(...).latest('time')` 再从 JSONField 取，混入日志表 |
| 耦合 | 解析缓存与原始日志解耦，回填命令只写新表 | 历史日志行被塞进解析数据，表越来越胖、备份/迁移更重 |
| 结论 | **推荐**：语义清晰、索引独立、回填简单（parse raw → 新表） | 表少一张，但概念混杂、查询别扭 |

> 两者都**保留 `CheckResult.result` 原文**作为重解析溯源（解析器升级后跑 `backfill_parse_results` 即可刷新 `DeviceParseResult`，无需重采）。

---

*文档版本 v2（评审定稿）— 2026-07-23*
