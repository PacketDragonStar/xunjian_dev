# 巡检系统 × network-seek：架构与协同教程（含架构点评）

> 适用：阶段 A/B/C/D 落地后的单引擎巡检系统 + network-seek 拓扑图。
> 本文档聚焦两件事：**①协同到底怎么实现的；②这套架构合不合理**。
> 日常完整操作手册见同目录 `巡检系统使用手册_从部署到拓扑联动.md`，本文是其「架构视角」补充与纠错版。

> ✅ **更新（2026-07-23）**：本文此前指出的头号短板「解析器双份且分叉」**已修复**——`xunjian/app02/parsers/comware.py` 已成为 Comware 解析单一真源，`sync_cmdb` 与 `network-seek` 的 `comware.py`（现为适配层）共用同一份。详见第 4 节「P0 已完成」。部署时注意：network-seek 通过环境变量 `XUNJIAN_APP02_DIR` 指向 xunjian 项目根目录（默认值即本机路径，换机器需重设）。

---

## 0. 一句话定位

- **xunjian（Django + MySQL）** = 采集 + 判定 + 存原始回显。它只负责把设备命令的 **raw 文本** 原样存进 `CheckResult` 表，并跑异常检查器。
- **network-seek（Python + Neo4j）** = 拓扑构建。它**不采集**，只消费 xunjian 产出的原始回显，解析成图。
- **两者唯一的真理来源 = MySQL 里的 `CheckResult` 表。** CMDB 台账和 Neo4j 图都是它的**派生数据**，随时可重建，丢了不慌。

---

## 1. 系统架构总览（两仓各管什么）

```
┌─────────────────────────────────────────────────────────────────┐
│  设备（102 台 H3C Comware：知识城 76 + 化龙 26）                    │
└───────────────┬─────────────────────────────────────────────────┘
                │  netmiko SSH（executor 线程池并发）
                ▼
┌─────────────────────────────────────────────────────────────────┐
│  xunjian_system1  (Django + MySQL)   ← 采集 + 判定 + 存 raw        │
│                                                                   │
│  executor.py ──▶ CheckResult(device, command, result)  ← 真理来源 │
│       │            │                                             │
│       │            ├──▶ 异常引擎 (custom_checks.py) → AnomalyRecord│
│       │            ├──▶ sync_cmdb (自己 parse 一遍) → CMDB 台账    │
│       │            └──▶ export_networkseek_fixtures → 落盘 fixture │
└─────────────────────────────────────────────────────────────────┘
                │  cmdb_fixtures/<站点>/<设备>.txt  （文件交接）
                │  !Command: <命令> 分段
                ▼
┌─────────────────────────────────────────────────────────────────┐
│  network-seek  (Python + Neo4j)   ← 不采集，只吃 xunjian 的产物     │
│                                                                   │
│  reimport_comware.py ──▶ split_fixture_comware ──▶ parse_*_comware │
│       │                  (再 parse 一遍 raw)       (pydantic IR)  │
│       ▼                                                       │
│  PhysicalLayerWriter / LogicalLayerWriter / SecurityLayerWriter │
│       │                                                       │
│       ▼                                                       │
│  Neo4j 图（知识城 7687 / 化龙 7688 两个容器）                     │
│       │                                                       │
│       ▼                                                       │
│  14 个 MCP 工具（路径追踪 / 影响面分析 …）                         │
└─────────────────────────────────────────────────────────────────┘
```

**一句话**：xunjian 把 raw 存库，network-seek 把 raw 再 parse 成图。中间靠「文件 fixture」交接。

---

## 2. 协同是怎么实现的（核心）

协同分三层契约，理解这三层就懂了：

### 契约点 1：`CheckResult` 表（数据库内，强契约）
每台设备每条命令的原始回显，以 `(device, command, result)` 存一行。这是两仓共享的唯一真源。
- xunjian 写它、读它；network-seek 虽不直接连这个库，但它的所有输入都源自这里。

### 契约点 2：`cmdb_fixtures/<站点>/<设备>.txt`（文件，弱契约）
`export_networkseek_fixtures` 命令把 `CheckResult` 读出来，每台设备写成一个 `.txt`，内部用
```
!Command: display version
<原始回显...>

!Command: display lldp neighbor-information list
<原始回显...>
```
分段。**当前 SECTIONS 导出 13 类命令**：
version / current-configuration / interface brief / lldp / vlan brief / ip routing-table /
**vrrp / irf / m-lag summary / link-aggregation verbose / security-zone / security-policy ip / remote-backup-group status**。

### 契约点 3：`!Command:` 段名 → network-seek 内部 key 的映射（最脆弱）
network-seek 的 `split_fixture_comware` 按 `!Command:` 切段后，靠 `_cmd_to_key` 把命令字符串映射成内部 key
（`display_version` / `display_lldp` / `display_mlag_summary` …），再调对应 `parse_*_comware`。
**这一段映射是口头约定、无版本号、无契约测试**——xunjian 改了命令串或 network-seek 的映射漂移，会**静默丢段**。

### 串联命令：`rebuild_topology`
xunjian 侧一条命令把上面三件事串起来：
```bash
./venv/Scripts/python.exe manage.py rebuild_topology --site 知识城 --push
```
内部依次：`sync_cmdb` → `export_networkseek_fixtures` →（--push 时）subprocess 调 network-seek 的
`scripts/reimport_comware.py` 清空 Neo4j 后全量重灌。

---

## 3. 教程：从采集到拓扑的完整操作

### 3.1 首次部署（干净库）
```bash
cd C:/Users/ZSS/Desktop/广期所项目/xunjian/xunjian111/xunjian_system1
./venv/Scripts/python.exe manage.py migrate
./venv/Scripts/python.exe manage.py seed_inspection --site 知识城
./venv/Scripts/python.exe manage.py seed_inspection --site 化龙
./venv/Scripts/python.exe manage.py runserver 0.0.0.0:8000
```
> 部署细节见 `巡检系统使用手册_从部署到拓扑联动.md` 第 2 节，本文不重复。

### 3.2 日常巡检（采集 + 异常判定）
1. 打开「新巡检」页 → 选检查集/设备 → 触发。后端 Django 后台线程异步跑，秒回 `task_id`，前端轮询进度。
2. 原始回显写 `CheckResult`（后续一切的来源）；异常写 `AnomalyRecord`（带 P0/P1/P2）。
3. 失败设备可在「任务中心 / 详情页」续跑。

### 3.3 刷新 CMDB + 拓扑（协同主流程）
```bash
# 只刷本地 CMDB 台账 + 导出 fixture（不碰 Neo4j）
./venv/Scripts/python.exe manage.py rebuild_topology --site 知识城

# 顺带把 Neo4j 图也刷新（需 Neo4j 在线 + settings 配好 NETWORK_SEEK_*）
./venv/Scripts/python.exe manage.py rebuild_topology --site 化龙 --push
```
> `--push` 前置条件（settings.py）：
> ```python
> NETWORK_SEEK_DIR = r'C:/Users/ZSS/Desktop/network-seek'
> NETWORK_SEEK_PYTHON = r'.../network-seek/venv/Scripts/python.exe'
> NEO4J_BOLT_URI = 'bolt://localhost:7687'   # 化龙用 7688
> NEO4J_USER = 'neo4j'
> NEO4J_PASSWORD = 'networkseek2024'
> ```

### 3.4 联动自检（用库里真实回显验证 parser）
你之前强调过"可以自检，巡检已采集很多原始回显"。做法是直连本机 MySQL 的 `CheckResult` 表，
把每条命令的真实回显喂给 network-seek 的 `parse_*_comware`，跑覆盖矩阵：
```bash
# 在 network-seek 仓内（用其 venv）
cd C:/Users/ZSS/Desktop/network-seek
python selftest_parsers_vs_mysql.py     # 读 xunjian 的 CheckResult，逐命令验证解析
```
自检能暴露：旧命令 100% 错误回显（验证改名正确）、某命令段被漏导出、parser 正则对真实格式不兼容等。
**建议每次大改 parser 后都跑一遍**，比等贴样本靠谱。

### 3.5 加一条新命令怎么接
分两种意图（详见手册第 8 节）：
- **情形 A 只做异常判定**：在 `seed_inspection` 的 `COMMAND_RULES` 或界面加 CheckItem + 写 `@register_checker`，完事，不动 CMDB/图。
- **情形 B 还想进台账/图**：当前需挂 4 个点——
  ① `seed_inspection` 加命令进 `CheckResult`；② `sync_cmdb` 加 `parse_*` 写台账；
  ③ `export_networkseek_fixtures.SECTIONS` 加一行；④ `network_seek/parsers/comware.py` 写 `parse_*_comware` + `_cmd_to_key` 登记 + `importer` 调 `build_*`。
  ⚠️ **这正是当前架构最累的地方**：一套命令要在两仓各写一遍正则（见第 4 节点评）。

---

## 4. 架构点评：合不合理？

### 结论先行
**方向对、设计文档对，且阶段一、阶段二均已落地。**
- 此前最大的短板「解析器双份且分叉」已修复：`xunjian/app02/parsers/comware.py` 现在是 Comware 解析**唯一真源**（纯 Python、无 Django 依赖），`sync_cmdb` 直接消费它；`network_seek/parsers/comware.py` 改为**适配层**，把同一份 dict 解析器包装成 pydantic IR，原有 `importer` / `builders` / MCP 工具零改动。新加的 M-LAG / VRRP / 安全命令也一并归并到此单一真源。
- **阶段二（采集时一次解析落库）也已落地（2026-07-23）**：`pipeline.run_check_item` 在采集时按命令 `parse_device_command` 一次解析并落 `DeviceParseResult`；`sync_cmdb` 优先消费结构化结果（无则实时 parse 回退）；`stage_c._parse_lldp` 复用单一真源；`check_agg`/`check_vlan`/`check_zone` 消费结构化（raw 回退）；`backfill_parse_results` 回填历史。raw 不再被 sync_cmdb 重解析，下游（CMDB/异常/拓扑）共用同一份结构化数据。
剩下的是阶段三（export 产结构化 JSON + network-seek 薄导入），属锦上添花，不紧急。

### 合理的地方（这些别动，是对的）
1. **关注点分离清晰**：采集（xunjian）和拓扑（network-seek）是两件不同的事，拆两个仓、两种存储（MySQL / Neo4j）是正确决策。
2. **CheckResult 当唯一真源**：CMDB 和图都是可重建的派生数据，丢了重跑即可，没有"源数据"焦虑。
3. **全量重置重建（clear_all + 重灌）**：无增量 diff 复杂度、无脏数据，百台规模完全够用，简单可靠。
4. **确定性 ETL，零 LLM**：图是确定正则解析出来的，可重复、零费用、可审计。
5. **IR + 图写入器复用**：H3C 直接复用 NX-OS 的 writer，14 个 MCP 工具零改动即可查 H3C 资产——这是很漂亮的抽象。

### 需要改的地方（按优先级）
| # | 问题 | 影响 | 优先级 |
|---|---|---|---|
| 1 | ~~解析逻辑双份且已分叉~~ ✅ **已解决（2026-07-23）**：`app02/parsers/comware.py` 已成唯一真源，两仓共用 | 改一遍只改一处 | 已完成 |
| 2 | **协同靠隐式文件契约**（`!Command:` + `_cmd_to_key`，无版本号、无契约测试） | 任一侧命令串/映射漂移 → 静默丢段 | **P1** |
| 3 | **新 HA/安全命令只进图、不进 CMDB 台账** | 台账视角看不到 M-LAG/VRRP/安全域，易误判 | **P1** |
| 4 | **全量清空再灌，无部分失败保护** | 导入中途崩 → 图变空（百台可接受，但要知道） | P2 |
| 5 | **两仓各自 venv、各自 DB、无 CI** | 解析器分叉之所以"静默"，就是没集成测试守契约 | P2 |

### 改进路线图（务实版，不动大结构）
- **✅ P0 — 单一真源（已完成，2026-07-23）**：`xunjian/app02/parsers/comware.py` 已建成 Comware 解析唯一真源（纯 Python），`sync_cmdb` 与 `network-seek` 的 `comware.py`（适配层）均消费它。验证：xunjian `manage.py check` 0 issues；network-seek 解析器单测 21/21 通过；core 适配层用样例跑通。附带修复一个潜在 bug：`display vlan brief` 的 `1(default)` 此前会被丢弃（VLAN 1 不进台账），现已修正且两仓同时受益。
  - ⚠️ **部署注意**：network-seek 通过环境变量 `XUNJIAN_APP02_DIR` 指向 xunjian 项目**根目录**（默认值为本机绝对路径）；换机器部署需重设该变量，否则 import 失败。
- **P1 — fixture 加 schema 版本号**（`!Schema: comware-fixture/v1`）+ 把自检脚本固化成 `rebuild_topology --check` 或 CI，守住契约不漂移。
- **P1 — HA/安全命令回灌 CMDB**（或至少在文档里明确"这些只进图"），避免台账盲区。
- **✅ P2 — 采集时一次解析落库（已完成，2026-07-23）**：`DeviceParseResult` 模型（迁移 `0006`）+ `parse_device_command` 调度层 + executor 落库 + `sync_cmdb` 消费结构化（实时 parse 回退）+ `stage_c` 复用单一真源 + `check_agg`/`check_vlan`/`check_zone` 消费结构化（raw 回退）+ `backfill_parse_results` 回填历史。验证：`manage.py check` 0 issues；调度层与三个结构化检查器 verdict 与 raw 路径一致；`app02/parsers` 仍纯 Python，network-seek 适配层零改动。
- **✅ P3 — export 产结构化 JSON + network-seek 薄导入（已完成，2026-07-23）**：`export_networkseek_fixtures` 默认产 `<device>.json`（含 `schema_version` + `parsed` 命令→规范化 dict，取自阶段二 `DeviceParseResult`，无则实时 parse 回退）；network-seek 新增 `import_comware_structured`/`full_reset_and_import_comware_json`，**直接消费结构化 dict 建图、不再 re-parse raw**；`rebuild_topology --push` 默认走 JSON，旧 `.txt` 路径保留为 `--legacy-fixture`/`reimport_comware.py --no-json` 回退。raw 与结构化两条路径经同一 `_import_comware_core` + 同一套 `_build_*_comware` 适配，Neo4j 节点/边数天然等价。验证：`manage.py check` 0 issues；network-seek 解析器单测 21/21 通过。
- **P2 — 图重建加"暂存子图 + 原子替换"**：避免清空后崩溃留空图。

---

## 5. 一句话总结
架构的**骨架（采集/拓扑分离、CheckResult 真源、确定性 ETL、IR 复用）很合理**，但**肌肉（解析器统一、契约固化）还没长好**。
最该马上做的是 P0：把两份正则合并成一份，让"改一遍只改一处"。这跟你 7/23 的决策完全一致，只是从设计稿变成代码而已。
