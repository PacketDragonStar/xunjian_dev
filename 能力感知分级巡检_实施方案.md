# 能力感知分级巡检（T1 基础层 + T2 能力层）实施方案

> 版本：v1（待 review）
> 目标：在现有「按角色模板」之上，新增「按设备实际启用协议」的能力门控，实现
> 「基础健康项全跑、特性项仅对启用该协议的设备执行」。

---

## 1. 背景与现状

### 1.1 当前架构（已确认）
- 巡检项由 `CheckItem` 定义；设备按「站点+角色」归入 `DeviceGroup`（5 套角色模板：FW/CSW/ASW/LSW/SRP）。
- 执行时 `app02/engine/executor.py:97 _get_items_for_device(device)` 是**全系统唯一**决定「该设备跑哪些项」的入口：
  ```python
  qs = device.group.check_items.filter(enabled=True)
  disabled = (device.extra or {}).get('disabled_commands') or []
  if disabled:
      qs = qs.exclude(command__in=disabled)
  ```
- `seed_inspection.py:261 ROLE_EXTRA_COMMANDS` 把 HA/特性命令按角色无差别注入：
  - CSW：`display irf` / `display m-lag summary` / `display link-aggregation verbose` / `display vrrp verbose`
  - FW：`display security-zone` / `display security-policy ip` / `display remote-backup-group status` / `display vrrp verbose`
- 这些特性命令对**同角色所有设备**都下发，没启用的设备靠 `prune_disabled_commands` 事后写入 `extra['disabled_commands']` 裁剪。

### 1.2 痛点
1. **噪声大**：大量设备回显「命令无结果/协议未开启」，被裁剪前先产生一堆无效 `CheckResult`。
2. **真机负载浪费**：对 102 台设备无差别跑 HA 命令，多数本就不支持。
3. **应执行数虚高**：`expected_count` 把 HA 项算进每台设备，统计失真。
4. **维护被动**：必须先「跑一次失败」才能被发现「没这协议」，是反向裁剪而非正向门控。

### 1.3 目标
引入能力门控（capability gating）：
- **T1 基础层 (`feature='base'`)**：同角色通用健康项，无脑全跑。
- **T2 能力层 (`feature=ospf/vrrp/bgp/irf/m-lag/rbm/security/lacp…`)**：仅当设备 `extra['capabilities']` 含该特性时才执行。

这是现有 `disabled_commands` 裁剪的**正向版**（「没配置的压根不跑」vs「跑失败的不跑」），两者互补、不冲突。

---

## 2. 总体设计

```
              DeviceGroup（站点+角色模板）
                    │
        ┌───────────┴───────────┐
        ▼                       ▼
   T1 base 项（恒执行）     T2 feature 项（能力门控）
                                  │
                                  ▼
                    设备 extra['capabilities'] = [ospf, vrrp, irf, ...]
                                  │
                                  ▼
                  _get_items_for_device 过滤：
                  Q(feature='base') | Q(feature__in=caps)
```

能力清单来源：**自动探测**（巡检开始时跑一条轻量探针命令，解析启用协议，缓存到 `extra['capabilities']`，并提供手动刷新命令）。

---

## 3. 数据模型变更

### 3.1 `CheckItem.feature`（新增字段）
`app02/models.py` 的 `CheckItem`（当前 92 行起）增加：
```python
FEATURE_CHOICES = [
    ('base',     '基础通用'),
    ('ospf',     'OSPF'),
    ('bgp',      'BGP'),
    ('vrrp',     'VRRP'),
    ('irf',      'IRF 堆叠'),
    ('m-lag',    'M-LAG'),
    ('rbm',      'RBM 备份组'),
    ('security', '安全域/策略'),
    ('lacp',     '链路聚合'),
]
feature = models.CharField(
    verbose_name='能力分类', max_length=20, default='base', choices=FEATURE_CHOICES,
    help_text='base=所有设备执行；其余=仅设备 capabilities 含该特性时执行'
)
```
- 旧数据迁移：`feature` 默认 `base` → **行为等同现状，零回归**。

### 3.2 `NewDevice.extra['capabilities']`（复用已有 JSON 字段）
- 类型：`list[str]`，如 `['ospf','vrrp','irf']`；**缺失该 key（None）= 从未探测**。
- 由探测命令填充（见 §4.4 `ensure_capabilities`），并附 `capabilities_ts`（ISO 时间，用于过期重探）。
- 语义：`None`=未探测→保守全跑；`[]`=已探测确无特性→仅 base；非空→按清单门控。
- 支持手动覆盖（前端勾选 / `discover_capabilities --force` 刷新）。

---

## 4. 能力探测（自动）

### 4.1 探针命令（单条覆盖，HP Comware）
```
display current-configuration | include ospf|bgp|vrrp|irf|m-lag|remote-backup-group|security-zone|link-aggregation|lacp|zone
```
> 说明：`display current-configuration | include` 是只读、非阻断命令，对设备几乎零负担，单条往返即可拿到全部协议开关。
> ⚠️ **include 过滤列表必须与 §4.4 `FEATURE_KEYWORDS` 的"主 token"保持单一真源**：include 里没列的关键字，回显根本不会返回，对应关键字永远命中不到（死代码）。建议 `PROBE_COMMAND` 由 `INCLUDE_TOKENS` 自动拼接（见 §4.4），不要手写两份。

### 4.2 关键字 → 能力映射（`app02/engine/capability.py`）
| 能力 | 命中关键字（配置行片段） | 备注 |
|------|--------------------------|------|
| ospf | `ospf` | 仅 `ospf` 主 token（`area` 不在 include，易误判他处，已弃用） |
| bgp  | `bgp` | 仅 `bgp`（`peer` 跨协议易误判，已弃用） |
| vrrp | `vrrp` | 仅 `vrrp`（`vrid` 不在 include，已弃用） |
| irf  | `irf` / `member` | `irf` 为主，`member` 仅辅助 |
| m-lag| `m-lag` | **严禁用 `neighbor`**——`neighbor` 同时是 BGP/OSPF 关键字，会误判非 M-LAG 设备 |
| rbm  | `remote-backup-group` | 仅主 token（`rbm` 不在 include） |
| security | `security-zone` / `zone` | `zone` 需加入 include |
| lacp | `link-aggregation` / `lacp` | `lacp` 需加入 include（`BAGG` 不在 include，已弃用） |

> 解析稳健性：
> - 用**词边界正则**避免误判（如 `irf` 不误匹配无关 token；多 token 型号 `SecPath M9000-X10` 已在 parser 兼容，此处同理）。
> - **所有关键字必须能在 §4.1 include 过滤后的回显中出现**，否则该关键字是死代码。新增能力时，include token 与 keyword 要同步加。
> - 留解析日志，便于校准。
> - 探针解析**宽松**：宁可多判（保守），也不漏判导致漏跑。

### 4.3 探测时机（A+B 结合）
- **巡检时（默认）**：`_xunjian_one_device` 在连接建立后、跑实际项前，先跑探针 → 解析 → 写回 `extra['capabilities']`（落库，单设备串行安全）→ 后续 feature 项按门控执行。
- **独立刷新命令**：`discover_capabilities` 批量刷新（见 §7），供「不想每次探测」或「手动校正」场景。
- **兜底**：若 `extra['capabilities']` 为空（既没探测也没刷新），`_get_items_for_device` 不门控 → 全跑（等同现状），绝不误伤。

### 4.4 新增模块 `app02/engine/capability.py`
```python
# 单一真源：include 过滤 token 与 FEATURE_KEYWORDS 主 token 必须一致（由这里派生 PROBE_COMMAND）
INCLUDE_TOKENS = ['ospf', 'bgp', 'vrrp', 'irf', 'm-lag',
                  'remote-backup-group', 'security-zone', 'link-aggregation', 'lacp', 'zone']
PROBE_COMMAND = "display current-configuration | include " + "|".join(INCLUDE_TOKENS)

FEATURE_KEYWORDS = {
    'ospf':     [r'\bospf\b'],
    'bgp':      [r'\bbgp\b'],
    'vrrp':     [r'\bvrrp\b'],
    'irf':      [r'\birf\b', r'\bmember\b'],
    'm-lag':    [r'\bm-lag\b'],          # 注意：绝不用 neighbor（会误判 BGP/OSPF）
    'rbm':      [r'remote-backup-group'],
    'security': [r'security-zone', r'\bzone\b'],
    'lacp':     [r'link-aggregation', r'\blacp\b'],
}

# 能力清单过期阈值（天）：超过则下次巡检自动重探，避免配置变更后能力滞留
CAP_STALE_DAYS = 7

def detect_capabilities(raw: str) -> list:
    """从探针回显解析能力清单（去重、保序）。"""
    caps = []
    for feat, pats in FEATURE_KEYWORDS.items():
        if any(re.search(p, raw, re.I) for p in pats):
            caps.append(feat)
    return caps

def probe_device_capabilities(connection) -> list:
    raw = connection.send_command(PROBE_COMMAND, read_timeout=30,
                                  strip_command=True, strip_prompt=True)
    return detect_capabilities(raw)

def ensure_capabilities(device, connection, force=False) -> list:
    """保证 device.extra['capabilities'] 最新；返回能力清单。

    - 未探测(None) 或 超过 CAP_STALE_DAYS 天 或 force → 重新探测并写回 extra。
    - 探测失败 → 返回已有的 caps（无则 []），不门控由调用方兜底。
    - 这是 executor 与探测逻辑的唯一耦合点（抽成独立函数，降低耦合）。
    """
    extra = dict(device.extra or {})
    caps = extra.get('capabilities')          # None=未探测；[]=确无特性
    ts = extra.get('capabilities_ts')
    stale = (ts is None) or force or _is_older_than(ts, CAP_STALE_DAYS)
    if caps is None or stale:
        try:
            caps = probe_device_capabilities(connection)
            extra['capabilities'] = caps
            extra['capabilities_ts'] = _now_iso()
            device.extra = extra
            NewDevice.objects.filter(pk=device.pk).update(extra=extra)
        except Exception as e:
            logger.warning(f'[{device.name}] 能力探测失败(不写库,保持未探测→保守全跑): {e}')
            return extra.get('capabilities')  # 可能为 None → 调用方走全跑兜底
    return caps
```

---

## 5. 执行链路改造

### 5.1 `_get_items_for_device`（executor.py:97）
```python
def _get_items_for_device(device):
    if device.group:
        qs = device.group.check_items.filter(enabled=True)
        # —— 能力门控（T2）——
        # None = 从未探测 → 保守全跑（等价现状，绝不误伤）
        # []   = 已探测、确无特性 → 仅 base（避免对无协议设备空跑 HA 项）
        caps = (device.extra or {}).get('capabilities', None)
        if caps is not None:
            qs = qs.filter(Q(feature='base') | Q(feature__in=caps))
        # —— 事后裁剪（保留）——
        disabled = (device.extra or {}).get('disabled_commands') or []
        if disabled:
            qs = qs.exclude(command__in=disabled)
        return qs
    return None
```
> `expected_count` / `InspectionGap`（均复用本函数）**自动跟随**：缺特性设备的应执行数自动收敛，统计变干净。
> 配合 §5.2：`ensure_capabilities` 探测成功后写入 `capabilities`（即便为空 `[]`），故后续 `_get_items_for_device` 走门控分支；探测失败则不写、保持 `None` → 保守全跑。

### 5.2 `run_xunjian` / `_xunjian_one_device`（executor 主流程）

> ⚠️ **时序硬约束（v1 版遗漏）**：当前 `run_xunjian` 在**主线程**就调 `_get_items_for_device(device)` 算好 `actual_items`（executor.py:474/489/512），再塞进 worker。若探针在 worker 里跑、却不重新计算 `actual_items`，**探针结果对本次门控完全无效，门控要等到下一次巡检才生效**。
> 正确做法：**探测后必须重新计算本设备本次应执行项**，再遍历。

- `_xunjian_one_device` 改为：连接建立 + 关分页后，先 `caps = ensure_capabilities(device, connection)`（内部写回 `device.extra`）→ **再** `items_qs = _get_items_for_device(device)`（此刻读到最新 `device.extra`，门控生效）→ `actual_items = list(items_qs)` → 遍历。
- 主循环传进来的 `check_items` 仅作为"兜底全集"参考；真正执行以探测后重算的 `actual_items` 为准。
- `ensure_capabilities` 失败 → 返回已有 caps（无则 `[]`）→ `_get_items_for_device` 见 `caps=[]` 仍按"已探测但为空"处理（见 §5.1 末：empty 也门控，仅 base 跑）。若需绝对保守，可在探测异常时强制 `caps=None` 走全跑——二选一，默认推荐"空也门控"（避免无谓重试）。

> 说明：首次巡检 `capabilities` 为 None → 探测 → 写回 → 本次即按门控执行（因已重算）。后续 `capabilities_ts` 未超 `CAP_STALE_DAYS` 则跳过探测、直接用缓存，省一次命令。

---

## 6. seed_inspection 标签化

`app02/management/commands/seed_inspection.py` 创建/更新 `CheckItem` 时按命令打 `feature`：

| 命令 | feature |
|------|---------|
| `display irf` | irf |
| `display m-lag summary` | m-lag |
| `display link-aggregation verbose` | lacp |
| `display vrrp verbose` | vrrp |
| `display remote-backup-group status` | rbm |
| `display security-zone` / `display security-policy ip` | security |
| （新增）`display ospf peer` / `display ospf brief` | ospf |
| （新增）`display bgp peer` / `display bgp summary` | bgp |
| CPU/内存/日志/接口/版本/风扇/电源等基础项 | base |

- **幂等**：已存在的 `CheckItem` 仅 `update feature`（不重建，不丢历史/基线）。
- `ROLE_EXTRA_COMMANDS` 保留（决定哪些角色注入哪些特性命令），新增 ospf/bgp 到 CSW/FW 的 `ROLE_EXTRA_COMMANDS`，并补充对应 `CheckItem`。

---

## 7. 新增管理命令 `discover_capabilities`

```
python manage.py discover_capabilities [--site 知识城|化龙|all]
                                       [--device fw001]
                                       [--dry-run] [--force]
```
- 逐设备 SSH 连接 → 跑探针 → `detect_capabilities` → 写 `extra['capabilities']`。
- `--dry-run` 只打印各设备能力分布，不写库。
- `--force` 覆盖已有清单；否则已有则跳过（避免重复探测）。
- 用途：① 巡检前一次性建立能力基线；② 配置变更后手动刷新；③ 校正自动探测偏差。

---

## 8. 与现有机制的关系

| 机制 | 角色 | 是否保留 |
|------|------|----------|
| `feature` 门控（新增） | 正向：没配置就不跑 | 新增 |
| `disabled_commands` 裁剪 | 反向：跑了失败才不跑（协议启用但采集异常） | 保留 |
| `expected_count` / `InspectionGap` | 统计「应执行/缺口」 | 自动跟随（复用入口） |
| `prune_disabled_commands` | 清理失败命令 | 保留（启用门控后，缺特性设备的 HA 项由 feature 过滤，不再进 disabled_commands，逻辑更清晰） |

---

## 9. 前端（可选增强）
- 设备详情页：展示 detected `capabilities`（只读 + 可手动勾选覆盖）。
- 巡检项列表：增加 `feature` 标签列（base / ospf / vrrp …）。
- 巡检历史：可按 `feature` 过滤/分组查看。

---

## 10. 实施步骤（建议顺序）
1. `CheckItem.feature` 字段 + 迁移 `0012_*`。
2. 新建 `app02/engine/capability.py` + 单测（多种回显样例）。
3. `_get_items_for_device` 加 feature 过滤（caps 空时保守全跑）。
4. `run_xunjian` 探测/读 `extra` 写入逻辑。
5. `seed_inspection` 标签化（含 ospf/bgp 项）。
6. `discover_capabilities` 命令。
7. 前端展示（可选）。
8. `manage.py check` + 单测 + 内网友好验证。

---

## 11. 风险与回退
- **风险**：探针在个别型号/版本回显格式不同 → 探测不准 → 漏跑 feature 项。
  - 缓解：caps 空时保守全跑；`discover` 手动校正；探针解析加日志；关键字用宽松匹配。
- **回退**：`feature` 默认 `base`，旧数据全为 `base` → 行为等同现状，**零回归**。删除 `feature` 字段即完全回退。
- **隔离性**：不影响现有 base 健康项、不影响 `disabled_commands`、不影响网络拓扑(nerwork-seek) 已有采集。

---

## 12. 验证计划
1. 单测 `detect_capabilities`：覆盖有/无各特性、多 token 型号、空回显。
2. 内网先 `discover_capabilities --dry-run` 看各设备能力分布是否合理（如 CSW 应多 irf/m-lag/vrrp，FW 应多 security/rbm/vrrp）。
3. 跑一次全量巡检，核对 `expected_count` 是否比改造前下降（缺特性设备不再计入 HA 项）。
4. 抽一台无 OSPF 的交换机，确认 `display ospf peer` **不进**其 `CheckResult`。
5. 核对 `InspectionGap` 缺口为 0（或仅真机断连导致）。

---

## 13. 评审修订记录（v1 → v2，2026-07-24 复查）

> 对照真实代码（executor.py 主循环、`_get_items_for_device`、seed）复查后修订。

### 13.1 三轴评估结论

**健壮性（Robustness）**
- 🔴 **HIGH·时序错误（已修 §5.2）**：原 §5.2 描述"连接后、遍历前探测→后续按门控执行"，但 `actual_items` 在主线程（executor.py:474/489/512）就已算好传入 worker，探针结果**对本次门控无效**，门控只能下次生效。v2 改为探测后**重算** `actual_items` 再遍历。
- 🔴 **HIGH·关键字/过滤脱节（已修 §4.1/§4.2/§4.4）**：原 include 仅 8 个 token，但 `FEATURE_KEYWORDS` 列了 `area/vrid/member/neighbor/rbm/security-policy/zone/lacp/BAGG` 等——其中多数不在 include 里，**回显根本不返回，成死代码**；且 `neighbor` 会**误判 BGP/OSPF 设备为 M-LAG**（真误报）。v2 收紧关键字、弃用 `neighbor`、include 由 `INCLUDE_TOKENS` 单一真源派生。
- 🟠 **MED·缓存无法自动刷新（已修 §4.4）**：原 `if not capabilities` 在 caps 非空后永不再探，配置变更（新增 OSPF）不被发现。v2 加 `capabilities_ts` + `CAP_STALE_DAYS`，过期自动重探。
- 🟠 **MED·`None` 与 `[]` 混淆（已修 §3.2/§5.1）**：原 `caps or []` 把"未探测"与"确无特性"混同，导致确认无特性的设备反而被全量跑。v2 区分 `None`(保守全跑) vs `[]`(仅 base)。
- 🟡 **LOW·超大配置超时**：`display current-configuration` 在超大型设备上可能慢；`read_timeout=30` 尚可，必要时可加退避或仅采 `| include` 过滤结果（已过滤，风险低）。

**适应性（Adaptability）**
- 🟠 **MED·特性命令仍绑 role（建议，未强改）**：`ROLE_EXTRA_COMMANDS` 按角色注入特性命令，与能力门控双轨。更干净的做法是 **feature 命令全局分配给所有 DeviceGroup，纯靠 capability 门控**，彻底解耦 role——新增协议不用再改 seed 的角色映射。
- 🟠 **MED·与命名规则分类脱节（建议，未强改）**：前序已发现 29 台 `role` 实际标错（OASW/PSW/USW 全塞 ASW、DCI/DSW 塞 LSW）。能力门控叠在错误的 role 之上，base 命令集与特性注入都会错。建议：**命名规则→`device_class` 作为 base 真源，capability 作 T2**，feature 命令不依赖 role（见上条）。
- 🟡 **LOW·加新协议成本**：`FEATURE_CHOICES` 是 CharField（不强制迁移，改 Python 即可），但仍要同步改 `INCLUDE_TOKENS` + `FEATURE_KEYWORDS` 两处；v2 已把 include 改为由 token 派生，降低遗漏。
- 🟡 **LOW·仅适配 hp_comware**：探针命令与关键字均 Comware 专属。当前项目全为 hp_comware（已确认），但方案应显式声明此前提。

**耦合性（Coupling）**
- 🟢 **GOOD·单入口 + 纯函数**：`_get_items_for_device` 是全系统唯一"决定跑哪些项"入口（低耦合）；`capability.py` 为纯函数（可单测、无副作用）。
- 🟠 **MED·探测逻辑塞进执行线程（已修 §4.4/§5.2）**：原 §5.2 把"探测+写库"直接写在 `_xunjian_one_device`。v2 抽成 `ensure_capabilities(device, connection)` 独立函数，executor 仅一处调用，耦合收敛。
- 🟡 **LOW·与 extra JSON 契约耦合**：探针直接读写 `device.extra['capabilities']`；若 extra 结构演进需同步。风险低（extra 本就是自由 JSON）。

### 13.2 落地前必改项（Checklist）
1. ☐ §5.2 探测后重算 `actual_items`（HIGH，已写进文档，实现时务必照做）
2. ☐ `INCLUDE_TOKENS` 单一真源派生 `PROBE_COMMAND`（HIGH，已写）
3. ☐ 弃用 `neighbor` 等易误判关键字（HIGH，已写）
4. ☐ `capabilities_ts` + 过期重探（MED，已写）
5. ☐ `None` vs `[]` 语义区分（MED，已写）
6. ☐ （建议）feature 命令全局化、与命名规则 `device_class` 衔接（结构性，待用户拍板）
7. ☐ 单测覆盖：邻居误判、过期重探、None/[]、include/keyword 同步断言
