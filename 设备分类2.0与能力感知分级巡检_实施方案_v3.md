# 设备分类 2.0 + 能力感知分级巡检 实施方案（v3）

> 版本：v3（opt-in 模型定稿，待 review 后落地）
> 前置：v1（初稿）→ v2（三轴评审修 4 硬伤）→ **v3（模型升级 + 命名规则接入 + 特性命令全局化）**
> 作者定稿依据：2026-07-24 与用户多轮 review，下列决策均已拍板

---

## 0. 版本演进与本次核心转变

| 版本 | 关键内容 |
|------|----------|
| v1 | 初稿：T1 base + T2 能力门控，每次巡检自动探针 |
| v2 | 三轴评审，修 4 个硬伤（探针时序 / 关键字脱节 / 缓存不刷新 / None·[] 混淆） |
| **v3** | **① 模型升级为 opt-in（基础恒跑 + 检测能力纯透明 + 显式开关启用协议巡检）<br>② 接入命名规则 → `device_class`，修 29 台错标，作 base 真源<br>③ 特性命令全局化 + 纯能力门控，解耦 role<br>④ IDC = 出口交换机，补命名规则映射** |

**v3 相对 v2 最本质的变化**：能力探测从「巡检时自动发生、自动门控」改为「**检测**」与「**执行**」两个互相独立的动作——

- **检测能力**：任何时候可手动触发，跑探针写 `capabilities`，**只展示、不执行**；
- **启用协议巡检**：设备级开关 `protocol_inspection`，**默认关**；开了之后，巡检才按 `capabilities` 实际跑特性命令。

> 这个模型是用户（2026-07-24）提出的，评审认为比 v2 自动探针**更可控、更透明、且天然规避「部分设备不支持探针」的风险**。

---

## 1. 设计总览（三层模型）

```
                命名规则 Excel（权威真源）
                        │ 解析设备名前缀
                        ▼
        T0  device_class（基础分类真源，修 29 台错标）
             ASW/CSW/FW/OASW/PSW/USW/DCI/DSW/IDC(出口)/SRP …
                        │ 决定 DeviceGroup 归属 + base 命令集
                        ▼
        DeviceGroup（站点 + device_class 模板）
              ┌─────────┴──────────┐
              ▼                    ▼
     T1 base 项（恒执行）      T2 feature 项（能力门控，需显式启用）
                                   │ 仅当 device.protocol_inspection == True
                                   ▼
                          读 extra['capabilities']（由「检测能力」动作写入）
                                   │
                                   ▼
              _get_items_for_device 过滤：
              Q(feature='base') | Q(feature__in=caps)
```

三层职责分离、互不掺和：
- **T0 `device_class`**：这设备**是什么类型**（决定基础命令集）。来自官方命名标准，非手写粗映射。
- **T1 `base`**：同类型设备**无脑全跑**的通用健康项（CPU/内存/日志/接口/版本/风扇/电源/VLAN/LLDP…）。
- **T2 `feature`**：ospf/vrrp/bgp/irf/m-lag/rbm/security/lacp，**仅当启用开关且该设备确实开启了该协议才跑**。

---

## 2. 核心模型：opt-in 能力感知（已拍板）

### 2.1 三个互相独立的动作

| 动作 | 触发方式 | 行为 | 是否影响巡检执行 |
|------|----------|------|------------------|
| **① 基础巡检** | 每次巡检自动 | 跑 `feature='base'` 项 | 是（恒执行） |
| **② 检测能力** | 设备页「检测能力」按钮 / `discover_capabilities` 命令 | 跑探针 → 写 `extra['capabilities']` → **界面展示识别结果** | **否（纯透明，只检测不执行）** |
| **③ 启用协议巡检** | 设备级开关 `protocol_inspection` | 开了之后，巡检时按 `capabilities` 门控执行 feature 项 | 是（仅对该设备） |

> 关键解耦：**② 检测** 与 **③ 执行** 分离。你可以先「检测能力」看看某台设备被识别出哪些协议（透明的），再决定要不要开「协议巡检」开关。检测失败也不会影响任何已运行的巡检。

### 2.2 开关粒度 = A（逐设备，已拍板）

`protocol_inspection` 是**逐设备开关**，默认全关。推荐节奏：先挑核心 `CSW`/`FW` 试点启用，验证稳了再铺开。

```python
# NewDevice.extra 结构（示例）
{
  "capabilities": ["irf", "lacp"],          # 由「检测能力」写入，纯展示
  "capabilities_ts": "2026-07-24T16:00:00", # 探测时间戳，过期自动重探
  "protocol_inspection": False,             # 逐设备开关，默认关
  "disabled_commands": [...]                # 既有裁剪，保留
}
```

> 备选 B（全局开关 + 逐设备覆盖）未采纳——A 与「先基础、想巡检协议再启用」的节奏最贴，可控性最高。

### 2.3 为什么比 v2 自动探针更可控（对照）

| 维度 | v2（每次自动探针） | v3（opt-in） |
|------|-------------------|--------------|
| 基础巡检 | 跑 | **永远跑**（安全、普适） |
| 探针何时跑 | 每次巡检自动跑 | **仅在「检测能力」动作或开关启用后跑** |
| 探针结果 | 直接决定本次跑啥 | 先写 `capabilities` 给你**看**，不立即执行 |
| 真跑协议命令 | 自动 | **你开 `protocol_inspection` 才跑** |
| 某台探针失败 | 可能影响门控判断 | **只回退那一台到基础巡检 + 界面提示，不波及全 fleet** |

**规避「设备不支持探针」风险**：探针是 `display current-configuration | include …`，标准 Comware 命令，fleet 全 hp_comware 支持率近 100%。即便个别设备不支持，opt-in 下它只是「检测失败 + 协议巡检不跑」，基础巡检照常——不会像自动模型那样把整批门控搞乱。

---

## 3. 命名规则 → `device_class`（修 29 台错标，作 base 真源）

### 3.1 命名规则表解析（权威真源）

用户提供的《命名规则.xlsx》是一份「设备命名缩写 → 类型(网络/安全/信安/IT) → 说明」对照表（BR/CSR/CSW/ASW/LSW/FW/WAF/IPS/IDS/SRP… 共 113 类）。解析规则：**取设备名首段字母作前缀 → 查表得类型**。

### 3.2 与现有 `role` 的比对（已实跑验证，发现 29 台错标）

按命名规则解析全部 102 台后，与当前 `role` 字段比对，发现：

- 大量 **OASW(9) / PSW(12) / USW(2)** 被错误塞进 `role=ASW`（按规则它们分别是带外二层接入 / 汇聚三层 / 专线三层，应各自独立）；
- 化龙那两台 `role=LSW` 实为 **`dci001`(DCI 数据中心互联)** 和 **`dsw001`(DSW DCI 交换机)**，归类错误；
- 知识城 6 台 `idc001/idc002` 当前 `role=ASW`，且**命名规则表原本没有 `IDC` 缩写**（见 3.4）。

→ 当前 `role` 是手写前缀硬映射（seed 里的 `role_of()`），与官方命名标准已漂移。

### 3.3 IDC = 出口交换机（用户确认，补映射）

用户明确：**IDC 代表出口交换机**（管出/入网边界流量的交换机），本质仍是**交换机类**。

→ 在命名规则映射中补 `IDC → 出口交换机`，归为交换机类：
- 基础命令集：复用交换机那套（CPU/内存/日志/接口/VLAN/LLDP），不特殊化；
- 特性命令：走 capability 自动探测——出口交换机通常有上联路由，**BGP/OSPF 一旦探测到自动巡检**，无需手工指定；
- UI 显示为独立 `IDC(出口交换机)` 标签，方便区分。

### 3.4 `device_class` 落地

```python
# NewDevice 新增字段（替代手写 role 作 base 真源）
DEVICE_CLASS_CHOICES = [
    ('ASW','接入交换机'), ('CSW','核心交换机'), ('LSW','轻量交换机'),
    ('OASW','带外接入交换机'), ('PSW','汇聚交换机'), ('USW','专线交换机'),
    ('DCI','数据中心互联'), ('DSW','DCI交换机'), ('IDC','出口交换机'),
    ('SRP','业务路由器/设备'), ('FW','防火墙'), ('WAF','WEB应用防火墙'),
    ('IPS','入侵防御'), ('IDS','入侵检测'), ('OTHER','其他'),
]
device_class = models.CharField(
    max_length=20, default='OTHER', choices=DEVICE_CLASS_CHOICES,
    db_index=True, verbose_name='设备分类(命名规则)'
)
```

- `role` 字段**保留**（展示旧值/兼容），但**不再作为 DeviceGroup 归属与命令分配依据**；`device_class` 取而代之。
- DeviceGroup 命名键从 `(site, role)` 改为 `(site, device_class)`，如 `GRP-化龙-CSW` 不变，但 `ASW` 拆成 `ASW/OASW/PSW/USW` 各自独立组。
- 提供 `reclassify_by_naming_rule` 管理命令：一键按命名规则重算全部设备 `device_class`，并输出「与旧 role 不一致」报告（即 `classify_result.txt` 的固化版）。

### 3.5 回退

`device_class` 默认 `OTHER` → 归入 `GRP-{site}-OTHER`（或兜底组），base 命令仍可跑（需为该组配基础命令）。删除字段即回退到 role 逻辑。

---

## 4. 特性命令全局化 + 纯能力门控（解耦 role）

### 4.1 撤销按角色注入

`seed_inspection.py` 的 `ROLE_EXTRA_COMMANDS`（按角色无差别注入 HA 命令）**撤销**。原因（用户已认可）：
- 角色是粗分类，「有没有配协议」是每台设备自己的事。`csw004` 没开 M-LAG 却因 role=CSW 被注入 `display m-lag summary` → 报错 → 靠 prune 事后裁（麻烦）；`oasw005` 是网关**实际配了 VRRP** 但 role=ASW → VRRP 命令根本没注入 → **漏检**。

### 4.2 feature 命令全局链接所有 DeviceGroup

seed 时，把每条 `feature` 类 `CheckItem` 的 M2M（`check_items`）**链接到所有 DeviceGroup**。这样所有组的命令池都含完整 feature 集，门控在 executor 过滤，而非 seed 时按角色挑。

### 4.3 加协议只加一条 CheckItem

改造后，新增一个协议巡检（如 BGP 邻居）= **只加一条 `CheckItem(feature='bgp')`**，所有真开了 BGP 且 `protocol_inspection=True` 的设备自动就跑——不用碰任何角色映射、不用改 seed 角色分支、不用重 seed 角色。

> 与 v2 对比：v2 仍是「ROLE_EXTRA_COMMANDS 注入 + 能力门控双轨」，能力门控只能收窄角色注入、不能拓宽，加协议还得回头改角色映射。v3 彻底单轨。

---

## 5. 数据模型变更汇总

| 变更 | 位置 | 说明 | 回退 |
|------|------|------|------|
| `CheckItem.feature` | models.py | `base`/`ospf`/`bgp`/`vrrp`/`irf`/`m-lag`/`rbm`/`security`/`lacp`，默认 `base` | 删字段即回退 |
| `NewDevice.device_class` | models.py | 命名规则解析结果，替代 role 作 base 真源 | 删字段回退 role |
| `NewDevice.extra['capabilities']` | 既有 JSON | 探针识别的能力清单；`None`=未检测 | 清 key 即回退 |
| `NewDevice.extra['capabilities_ts']` | 既有 JSON | 探测时间戳，过期重探 | 清 key |
| `NewDevice.extra['protocol_inspection']` | 既有 JSON | **逐设备开关，默认 False** | 清 key（=关） |
| `DeviceGroup` 键 | seed | `(site, role)` → `(site, device_class)` | 重建组 |

> 旧数据迁移：`feature` 默认 `base`、`device_class` 由 `reclassify` 命令重算、`protocol_inspection` 默认关 → **初始行为等同现状（只跑 base + 无门控），零回归**。

---

## 6. 能力探测（探针 + 解析，吸收 v2 硬伤修复）

### 6.1 探针命令（单一真源派生）

```python
INCLUDE_TOKENS = ['ospf','bgp','vrrp','irf','m-lag',
                  'remote-backup-group','security-zone','link-aggregation','lacp','zone']
PROBE_COMMAND = "display current-configuration | include " + "|".join(INCLUDE_TOKENS)
```
> ⚠️ `PROBE_COMMAND` **必须由 `INCLUDE_TOKENS` 派生**，禁止手写两份——v2 的「include 8 个 token 但 keyword 列一堆死代码」硬伤即源于此。

### 6.2 关键字 → 能力映射（`capability.py`）

| 能力 | 命中关键字 | 备注 |
|------|-----------|------|
| ospf | `\bospf\b` | 仅主 token |
| bgp | `\bbgp\b` | 仅主 token（`peer` 跨协议易误判，弃用） |
| vrrp | `\bvrrp\b` | 仅主 token（`vrid` 不在 include，弃用） |
| irf | `\birf\b` / `\bmember\b` | `irf` 为主 |
| m-lag | `\bm-lag\b` | **严禁 `neighbor`**（BGP/OSPF 也用，会误判） |
| rbm | `remote-backup-group` | 仅主 token |
| security | `security-zone` / `\bzone\b` | `zone` 已加入 include |
| lacp | `link-aggregation` / `\blacp\b` | `lacp` 已加入 include（`BAGG` 弃用） |

### 6.3 解析与缓存（吸收 v2 修复）

```python
CAP_STALE_DAYS = 7

def detect_capabilities(raw: str) -> list:
    caps = []
    for feat, pats in FEATURE_KEYWORDS.items():
        if any(re.search(p, raw, re.I) for p in pats):
            caps.append(feat)
    return caps

def ensure_capabilities(device, connection, force=False) -> list:
    """仅在「检测能力」或「开关启用后巡检」时调用。
    - caps is None 或超 CAP_STALE_DAYS 或 force → 重新探测并写回 extra。
    - 探测失败 → 返回已有 caps（无则 []），不门控由调用方兜底。
    """
    extra = dict(device.extra or {})
    caps = extra.get('capabilities')          # None=未检测；[]=确无特性
    ts = extra.get('capabilities_ts')
    stale = (ts is None) or force or _is_older_than(ts, CAP_STALE_DAYS)
    if caps is None or stale:
        try:
            caps = probe_device_capabilities(connection)
            extra['capabilities'] = caps
            extra['capabilities_ts'] = _now_iso()
            device.extra = extra
            NewDevice.objects.filter(pk=device.pk).update(extra=extra)
        except Exception as e:
            logger.warning(f'[{device.name}] 能力探测失败(不写库): {e}')
            return extra.get('capabilities')  # None → 调用方走基础兜底
    return caps
```

> **`None` vs `[]` 语义（v2 已修）**：`None`=从未检测→保守全跑；`[]`=已检测确无特性→仅 base。两者严格区分，避免「确认无 OSPF 的交换机反而被全量跑 HA 项」。

### 6.4 真实样例验证（用户贴回显）

`csw003&004.pri.2IDC4f.hualong.xc` 探针回显含 `irf …` 与 `link-aggregation …`，**不含** ospf/bgp/vrrp/m-lag/rbm/security-zone → 解析为 `['irf','lacp']`，其余自动跳过。与设想完全一致 ✅

---

## 7. 执行链路改造（opt-in 版）

### 7.1 `_get_items_for_device`（executor.py:97）

```python
def _get_items_for_device(device):
    if device.group:
        qs = device.group.check_items.filter(enabled=True)
        extra = device.extra or {}
        # —— T2 能力门控（需显式启用开关）——
        if extra.get('protocol_inspection'):
            caps = extra.get('capabilities', None)
            if caps is not None:                      # 已检测才门控；未检测(None)→ 仅 base
                qs = qs.filter(Q(feature='base') | Q(feature__in=caps))
            # caps is None（没检测过）→ 不门控，等同只跑 base 也安全
        # —— 事后裁剪（保留）——
        disabled = extra.get('disabled_commands') or []
        if disabled:
            qs = qs.exclude(command__in=disabled)
        return qs
    return None
```

### 7.2 `run_xunjian` / `_xunjian_one_device`（主流程）

> ⚠️ **时序约束（v2 已修）**：`actual_items` 在主线程算好传入 worker，若探针在 worker 跑却不重算，门控无效。故探测后**必须重算** `actual_items`。

```python
# _xunjian_one_device 连接建立 + 关分页后：
extra = device.extra or {}
if extra.get('protocol_inspection'):
    caps = ensure_capabilities(device, connection)   # 内部写回 extra['capabilities']
    # 探测后重算本设备本次应执行项（v2 时序修复）
    items_qs = _get_items_for_device(device)          # 此刻读到最新 extra，门控生效
    actual_items = list(items_qs)
else:
    actual_items = list(_get_items_for_device(device))  # 仅 base（不探、不跑 feature）
```

- `protocol_inspection=False`（默认）：只跑 base，连探针都不发，**省一次命令、零风险**。
- `protocol_inspection=True`：先确保 capabilities（过期则重探）→ 重算 → 按门控执行 feature 项。
- 探测失败：`ensure_capabilities` 返回 `None` → `_get_items_for_device` 走「未检测」分支 → 仅 base，该设备本次不跑任何 feature（安全回退，下次可手动「检测能力」校正）。

---

## 8. seed_inspection 改造

| 改动 | 内容 |
|------|------|
| 撤销 `ROLE_EXTRA_COMMANDS` | 不再按角色注入特性命令 |
| `device_class` 解析 | 用命名规则映射（含 `IDC→出口交换机`）算每台 `device_class`，建 `(site, device_class)` DeviceGroup |
| `feature` 标签化 | 创建/更新 `CheckItem` 时按命令打 `feature`（见下表） |
| feature 全局链接 | 每条 feature `CheckItem` 的 M2M 链接到**所有** DeviceGroup |
| 基础命令按类分配 | 不同 `device_class` 的 base 命令集可不同（交换机含 VLAN/LLDP，FW 含安全基础等） |
| 幂等 | 已存在项仅 `update`，不重建，不丢历史/基线 |

`feature` 标签对照（新增 ospf/bgp 两项）：

| 命令 | feature |
|------|---------|
| `display irf` | irf |
| `display m-lag summary` | m-lag |
| `display link-aggregation verbose` | lacp |
| `display vrrp verbose` | vrrp |
| `display remote-backup-group status` | rbm |
| `display security-zone` / `display security-policy ip` | security |
| （新增）`display ospf peer` / `display ospf brief` | ospf |
| （新增）`display bgp peer` / `display bgp summary` | bgp |
| CPU/内存/日志/接口/版本/风扇/电源/VLAN/LLDP 等 | base |

---

## 9. 管理命令

```
# ① 按命名规则重分类（修 29 台错标，输出不一致报告）
python manage.py reclassify_by_naming_rule [--site 知识城|化龙|all] [--apply]

# ② 检测能力（纯透明，只写 capabilities 不执行）
python manage.py discover_capabilities [--site ...] [--device fw001] [--dry-run] [--force]

# ③ 启用/停用协议巡检（逐设备开关）
python manage.py set_protocol_inspection --device csw001 --on
python manage.py set_protocol_inspection --site 化龙 --role CSW --on   # 批量试点
```

---

## 10. 前端（必要增强）

- **设备详情页**：
  - 展示 `device_class` 标签（如 `IDC(出口交换机)`）；
  - 「检测能力」按钮 → 调 `discover_capabilities --device x` → 展示识别出的 `capabilities`（只读、可手动覆盖）；
  - `protocol_inspection` 开关（默认关），开/关即时生效。
- **巡检项列表**：增加 `feature` 标签列（base / ospf / vrrp …）。
- **巡检历史**：可按 `feature` 过滤/分组；`expected_count` 已随门控自动收敛。

---

## 11. 与现有机制关系

| 机制 | 角色 | 是否保留 |
|------|------|----------|
| `feature` 门控（新增） | 正向：没配置 + 没开开关 → 不跑 | 新增（opt-in） |
| `protocol_inspection` 开关（新增） | 控制是否按能力门控执行 | 新增（逐设备） |
| `device_class`（新增） | base 真源，替代手写 role | 新增 |
| `disabled_commands` 裁剪 | 反向：跑了失败才不跑 | 保留 |
| `expected_count` / `InspectionGap` | 统计「应执行/缺口」 | 自动跟随（复用入口） |
| `prune_disabled_commands` | 清理失败命令 | 保留 |

---

## 12. 实施步骤（分阶段，含回退点）

**阶段 0（独立、低风险，先修数据质量）**
1. `NewDevice.device_class` 字段 + 迁移；
2. `reclassify_by_naming_rule` 命令（先 `--apply` 前用 dry-run 校对 29 台）；
3. 应用重分类，DeviceGroup 按 `(site, device_class)` 重建。

**阶段 1（解耦 role）**
4. `CheckItem.feature` 字段 + 迁移；
5. `capability.py` 模块 + 单测（吸收 v2 硬伤）；
6. seed 撤销 `ROLE_EXTRA_COMMANDS`、feature 全局链接、标签化（含 ospf/bgp）。

**阶段 2（opt-in 执行）**
7. `_get_items_for_device` 加 `protocol_inspection` + feature 过滤；
8. `run_xunjian` 探测/读 extra 逻辑（含时序重算）；
9. `discover_capabilities` / `set_protocol_inspection` 命令；
10. 前端设备页（device_class 标签 + 检测能力 + 开关）。

**阶段 3（验证）**
11. `manage.py check` + 单测 + 内网友好验证。

> 每阶段可独立回退，互不阻塞。阶段 0 不依赖阶段 1/2，可先单独上线修数据。

---

## 13. 风险与回退

- **风险1·探针个别设备不支持**：opt-in 下仅该设备「检测失败 + 协议巡检不跑」，基础巡检不受影响，不波及全 fleet。缓解：先 dry-run 看分布。
- **风险2·探测误判导致漏跑 feature**：`None`→保守只跑 base；`discover_capabilities --force` 手动校正；关键字宽松匹配 + 日志。
- **风险3·device_class 重分类误判**：`reclassify` dry-run 先出报告人工校对；`OTHER` 兜底组仍可跑 base。
- **回退**：`feature` 默认 `base`、`device_class` 可回退 role、`protocol_inspection` 默认关 → 初始行为等同现状，**零回归**。删除三处新增即完全回退。

---

## 14. 验证计划

1. 单测 `detect_capabilities`：覆盖有/无各特性、`csw003&004` 真实回显样例、多 token 型号、空回显。
2. `reclassify_by_naming_rule --dry-run`：核对 29 台不一致报告是否合理（OASW/PSW/USW 拆分、DCI/DSW 归位、IDC 出口）。
3. `discover_capabilities --dry-run`：看各设备能力分布（CSW 多 irf/m-lag/vrrp，FW 多 security/rbm/vrrp）。
4. 试点：对 1~2 台 `CSW` 开 `protocol_inspection` → 跑巡检 → 确认仅对应 feature 项进 `CheckResult`，无 OSPF 命令不出现。
5. 核对 `expected_count` 比改造前下降（缺特性/未开开关设备不计入 feature 项）。
6. 核对 `InspectionGap` 缺口为 0（或仅真机断连）。

---

## 15. 评审修订记录（v2 → v3，2026-07-24 定稿）

### 15.1 模型升级
- **opt-in 取代自动探针**：v2「每次巡检自动探针 + 自动门控」→ v3「基础恒跑 + 检测能力(纯透明) + protocol_inspection 逐设备开关(默认关)」。用户拍板，评审认可更可控透明、规避不支持风险。

### 15.2 命名规则接入
- 新增 `device_class`（命名规则解析，含 `IDC=出口交换机` 补映射），作 base 真源，修 29 台 `role` 错标。DeviceGroup 键 `(site, role)` → `(site, device_class)`。

### 15.3 特性命令全局化
- 撤销 `ROLE_EXTRA_COMMANDS` 按角色注入；feature 命令全局链接所有 DeviceGroup，纯靠 capability 门控。加协议 = 只加一条 `CheckItem`。

### 15.4 吸收 v2 已修硬伤（不回退）
- 探针时序（探测后重算 `actual_items`）；
- `INCLUDE_TOKENS` 单一真源派生 `PROBE_COMMAND`；
- 弃用 `neighbor` 等易误判关键字；
- `capabilities_ts` + 过期重探；
- `None` vs `[]` 语义区分。

### 15.5 落地前 Checklist
1. ☐ `device_class` 字段 + 迁移 + `reclassify` 命令（阶段 0）
2. ☐ `CheckItem.feature` + 迁移 + `capability.py` 单测（阶段 1）
3. ☐ seed 撤销 ROLE_EXTRA_COMMANDS + feature 全局链接（阶段 1）
4. ☐ `_get_items_for_device` 加 `protocol_inspection` + feature 过滤（阶段 2）
5. ☐ `run_xunjian` 探测后重算 `actual_items`（HIGH）
6. ☐ `discover_capabilities` / `set_protocol_inspection` 命令（阶段 2）
7. ☐ 前端设备页（device_class + 检测能力 + 开关）（阶段 2）
8. ☐ 单测覆盖：邻居误判、过期重探、None/[]、include/keyword 同步、opt-in 开关分支
