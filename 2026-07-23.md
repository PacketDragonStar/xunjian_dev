
## 四、network-seek 识别 M-LAG / 堆叠 / VRRP / 防火墙策略（本次任务）
- 用户要求 network-seek 拓扑图能识别这四类。排查发现根因有二：(1) network-seek 的 comware parser 从未解析这些命令；(2) `export_networkseek_fixtures.py` 只导出 6 条命令，**连已在采集集的 vrrp/security-policy/zone 都没导出**，network-seek 根本拿不到。
- 已落地改动（两仓）：
  - xunjian：seed_inspection 新增 4 条 COMMAND_RULES + 按角色(CSW/FW)自动注入高可用/安全域命令；export SECTIONS 扩到 14 条；executor 过滤 `disabled_commands`；新增 `prune_disabled_commands` 管理命令实现"首次全量采集后，未开启协议的设备自动停采该命令"。
  - network-seek：ir 新增 SecurityZone/SecurityPolicy；comware parser 新增 6 个 parse 函数 + _cmd_to_key 映射；builders 补 zone/policy 边；importer 接线 `_import_comware_ha_and_security`。
- 验证：xunjian `manage.py check` 0 issues；network-seek 新增 parser 单测 13/13、既有 40/40 无回归。
- **待办（阻塞上线）**：parser 用 Comware V7 文档格式写、缺真实回显，需用户贴 1 台 CSW(display irf / m-lag summary / link-aggregation verbose / vrrp) + 1 台 FW(display security-zone / security-policy ip rule all / rbm / vrrp) 真实输出校准。部署顺序：内网 seed_inspection → 全量巡检 → prune_disabled_commands → export_networkseek_fixtures → network-seek 导入。
